# JARVIS AI OS

Privacy-first, plugin-based agent kernel for Linux. Pre-alpha. M0 (v0.1.1), M1 (v0.2.0), and M2 (v0.3.0) are tagged; M3 is code-complete (WP-43 through WP-56) but not yet tagged; M4 is code-complete (WP-57 through WP-64, plus the WP-65 M4-gap-closure pass) and tagged v0.4.0; M5 is code-complete (WP-67 through WP-75) and tagged v0.5.0 — both M4 and M5 tagged out of strict milestone-sequential order, since M3's own tag remains a deliberately separate, later action neither pass touched — see "Current Status" below for what's actually verified vs. still open at each milestone.

## Baseline
`docs/architecture/` and `docs/adr/` are approved and frozen. They are the source of truth, not this file. `m0-architecture.md`, `system-design.md`, and `m2-reasoning-layer.md` now exist (reconstructed 2026-08-18 from recovered fragments of the original design conversation and reconciled against the real repo — real ADRs, real code, real import-linter config). State plainly: these are reconciled reconstructions, not pristine originals; each documents its own gaps between what was decided and what was actually built. `m2-reasoning-layer.md` is recovered and was reconciled against what M0 became during the WP-28 planning pass (2026-08-18) — see that document's own status note and ADR-0038 through ADR-0043 for what the reconciliation actually decided; the recovered fragments themselves are not rewritten, deviations are recorded in the real ADRs and in `docs/threat-model/v0.md`'s "Milestone 2 additions" section instead. `m1-voice-architecture.md` is current and was kept in sync with implementation. `m3-desktop-control.md` is a real design (written 2026-08-21, not recovered from anything — no original fragments for M3 were ever found) and has now been implemented (WP-43 through WP-56); ADR-0044 through ADR-0047 record its own real, pre-implementation decisions (SandboxPort/bwrap, the Claude/ChatGPT ordinary-control boundary, Terminal's narrow no-shell exception, and the RemoteDesktop-portal mechanism for Terminal's own synthetic typing). `m4-memory-retrieval.md` is likewise a real design (written 2026-08-25, no original fragments for M4 either) and has now been implemented (WP-57 through WP-64); ADR-0048 through ADR-0054 record its own real, pre-implementation decisions (the two new ports, write-time and retrieval-time SECRET handling, 90-day default retention, the two no-indicator decisions, and the ClockPort/IdPort gap found mid-implementation — see `docs/threat-model/v0.md`'s "Milestone 4 additions" for the one real process deviation in how ADR-0054 was originally accepted, and "ADR-0054 ratified" for its 2026-09-04 retroactive ratification directly by the user). `m5-browser-coding.md` is a real design (written 2026-08-31, not recovered from anything — no original fragments for M5 either, and drafted from remotely-reasoned working assumptions rather than confirmed by the user directly in conversation, unlike M0 through M4's own design docs — see that document's own header) and has now been implemented (WP-67 through WP-75); ADR-0055 and ADR-0056 record its own real decisions (the coding-loop wrapper wraps M2's `Dispatcher` unmodified rather than retrofitting it; `Effect.CODE_WRITE`/`Effect.PROTECTED_PATH_WRITE` and an unconditional DENY floor for protected test paths) — both **Accepted**, but stated plainly and not smoothed over: accepted on this session's own judgment that a user-set bar (build WP-70/71/73, prove each one's required safety property, only then accept) had been met, not the user's own independent reading of the final ADR text — see `docs/threat-model/v0.md`'s "Milestone 5 additions" section, specifically the "ADR-0055 and ADR-0056 were accepted by relayed instruction" note, for the full, unrounded statement of that distinction. `m6-integrations.md` now covers both M6a and M6b via real design docs (M6a's own section points at `m6a-communications.md`; M6b's own section, updated 2026-09-02, points at `m6b-job-assistance.md`) — both ADRs (0057 for M6a, 0058 for M6b) are now Accepted, each directly by the user, in conversation, after direct review of its own full text; both M6a and M6b are now code-complete, both halves. `m6a-communications.md` is a real design (written 2026-09-01, not recovered from anything — no original fragments for M6a either, drafted from a mix of the user's own direct scoping answers and this pass's own remotely-reasoned classification decisions, later reviewed and confirmed by the user directly on 2026-09-03 — see that document's own header) — both its read-only ports/adapters (WP-76 through WP-80) and its write half (WP-79 onward, `send_message`/`create_event`) are implemented and tested. `docs/ROADMAP.md` is the real roadmap; a bare `roadmap.md` file does not exist.

## Current Status
M0 complete, tagged v0.1.1. M1 (voice) is code-complete, tagged v0.2.0 — all 7 gates pass on `main`'s tip today. **Correction, Track 6 health pass, 2026-09-02**: the exact tagged commit's own CI run was genuinely red (both matrix jobs failed at `Install dependencies` -- a missing PyGObject system-dependency step, WP-24, unrelated to any gate or test), not fixed until three days later (`8c1c6cef`); `main` has been green on every push since -- see `docs/threat-model/v0.md`'s own new note in "Milestone 1 additions" for the full, real finding. See also `docs/architecture/m1-voice-architecture.md`. Live end-to-end voice verification is still an open item (tracker #19). **Updated 2026-08-25 (overnight live-reality audit)**: every individual pipeline stage's own real mechanics — mic capture, wake-word model scoring, VAD, STT (real CUDA), TTS — were exercised live against real hardware and confirmed working correctly in isolation; this narrows tracker #19 (the bug is not in any single stage's basic mechanics) but does not close it — actual wake-word *detection accuracy* and the full `run_voice_loop` integration still need a physically-present human saying the wake phrase, which no unattended pass can supply. See `docs/threat-model/v0.md`'s "Overnight live-reality audit" section for the real findings. M2 (reasoning layer) is code-complete (WP-31 through WP-40; WP-41 deferred to M5 by design) — all gates pass including the `application/reasoning` 100%-branch-coverage gate (ADR-0041) — see `docs/architecture/m2-reasoning-layer.md` and `docs/threat-model/v0.md`'s "Milestone 2 additions" for what was actually built and the real, explicitly-accepted gaps (candidate execution is not sandboxed; cloud-provider adapters and the local-model adapter are code-complete but only partially live-verified against a real provider). M3 (desktop control) is code-complete (WP-43 through WP-56) — all gates pass, all eight target applications (Spotify, Brave, VS Code, Terminal, Docker, Git, the Claude desktop app, the ChatGPT desktop app) have real, typed, capability-gated support plus a real `SandboxPort`/`bwrap` containment mechanism — see `docs/architecture/m3-desktop-control.md` and `docs/threat-model/v0.md`'s "Milestone 3 additions"/"Live desktop-control verification"/"Overnight live-reality audit" sections for what was actually built and verified. **This is now substantially live-verified, not the blanket unverified gap once stated here**: Docker (`list_containers`) and Git (push/branch/commit/force-push) have been exercised for real against a real daemon and a real, network-reachable remote; Brave (`open_url`), VS Code (`open_file`), Spotify (MPRIS `play`/`pause`), and Terminal's real display have all been exercised for real against live desktop windows, across several sessions including one overnight pass that also found and fixed a real bug in `focus()`/`is_focused()`/`is_visible_and_showing()` (all three checked the wrong AT-SPI2 node — see the threat model). What remains genuinely unverified, stated precisely rather than rounded up: Terminal's synthetic-typing mechanism (ADR-0047, `SyntheticInputPort`) is code-complete but its real portal call has never fired — it needs the user physically present for the one real permission dialog, deliberately never triggered unattended; the Claude desktop app launches for real but exposes no AT-SPI2 tree (Electron/accessibility-bridge limitation, not a JARVIS bug); the ChatGPT desktop app was never installed (no official Linux client exists). `docker run`/`build_image` (DESTRUCTIVE tier) remain deliberately never exercised, matching this project's own non-negotiable rule about unattended destructive actions. M4 (memory & retrieval) is code-complete (WP-57 through WP-64) — all gates pass, real ports (`MemoryWritePort`, `RetrievalPort`, `EmbeddingPort`), a real SQLite-backed adapter (`SqliteMemoryAdapter`) using brute-force cosine similarity over `fastembed`/ONNX embeddings, and real, invocable kernel capabilities (`memory.write` via a dynamic-effect `MemoryWriteAuthorizer`, `memory.retrieve`/`memory.pin`/`memory.forget` as static capabilities — the latter two added in the WP-65 M4-gap-closure pass below) — see `docs/architecture/m4-memory-retrieval.md`, `docs/architecture/m4-benchmark-results.md`, and `docs/threat-model/v0.md`'s "Milestone 4 additions" for what was actually built. **Built in one unattended overnight pass — one real process deviation, named plainly**: ADR-0048 through ADR-0053 were accepted directly by the user before the pass began, but ADR-0054 (ClockPort/IdPort, a foundational gap found mid-implementation) was accepted unilaterally by the pass itself, not by the user — flagged for the user's own retroactive review, not presented as equivalent to the other six. Real, explicitly-named gaps, none rounded up to "done": ADR-0050's provenance carry-forward rule now has a real consumer (`application/memory/carry_forward.py`, closed 2026-09-02 -- see the threat model's own note) so that gap is closed; the `str`-only memory value limitation was narrowed 2026-09-02 (overnight Track 5 pass -- JSON-serializable non-str values, `int`/`float`/`bool`/`None`/`list`/`dict`, now real, supported memories; no real caller yet passes one, though -- see the threat model's own note); voice-triggered recall was closed 2026-09-02 (a real "recall <query>" command, kernel/intent.py + kernel/voice_loop.py -- see the threat model's own note); the full real write/retrieve pipeline was verified live exactly once, manually, never repeated by CI by design (matching this project's established precedent for network/hardware-dependent adapters). **Updated 2026-08-31 (WP-65, M4-gap-closure pass)**: the four gaps named at M4 closeout as separate, owed work are now closed — `MemoryWritePort.sweep_expired()` real GC sweep (triggered on every granted write), `authorize_and_forget()`/`MemoryWritePort.forget()` (a real "forget X" capability, `Effect.DESTRUCTIVE | Effect.IRREVERSIBLE`, `Tier.MANUAL_ONLY`, same combination as `git.force_push`), `authorize_and_pin()` (`memory.pin`'s first real caller), and real `jarvis memory write/retrieve/forget/pin` subcommands in `jarvis.cli.main`. All four kernel-level per this pass's own fixed scope — no new voice grammar added. One real correction made along the way, not a new gap: the original "no CLI subcommand... mirrors M3's own `docker.*`/`git.*` precedent" reasoning was itself a misreading (Docker/Git were simply never wired in, not following a real "no CLI" convention) — see `docs/threat-model/v0.md`'s "M4-gap-closure pass" note for the full correction. See that same note for what was closed vs. what remains open. **M4 tagged v0.4.0 2026-08-31**, immediately after WP-65's gates went green — see this file's own opening line for the tag-sequencing note (M3 remains untagged). M5 (browser automation + coding agent + minimal Console UI) is code-complete (WP-67 through WP-75) — all gates pass, real, live-verified browser automation (`CdpBrowserAutomationAdapter`, a real headless Brave instance, a real captured screenshot, a real DOM query, all proven live on the development machine), a real, end-to-end-proven coding-agent authorization chain (`Effect.CODE_WRITE`/`Effect.PROTECTED_PATH_WRITE`, ADR-0056; a disposable, `SandboxPort`-backed workspace per `Dispatcher.run()` climb, ADR-0055's own amendment; a real, finite-retry-budget coding-loop wrapper, `run_coding_task`; `coding.run_task` registered as the outer authorization gate, `kernel/coding.py`), and a real, minimal Console UI mechanism (`ConsolePort`/`GtkConsoleAdapter`, wired into `browser.open_page`) — see `docs/architecture/m5-browser-coding.md` and `docs/threat-model/v0.md`'s "Milestone 5 additions" for what was actually built and verified. **Real, explicitly-named gaps, none rounded up to "done"**: **Updated 2026-09-04** — `coding.run_task` now has a real, local-only default `dispatcher_factory` (`_local_only_dispatcher_factory`, `kernel/coding.py`), live-verified against a real, locally running Ollama server (`qwen2.5:0.5b`) and now wired into a real CLI caller (`jarvis code <task> <repo-path>`); a real cloud-provider default remains undecided by any ADR and was not built, only the local-only one, per the user's own explicit instruction — an explicit `dispatcher_factory` override still reaches real cloud escalation for a caller that supplies one directly. Voice grammar for `coding.run_task` was closed 2026-09-02 (overnight Track 4 pass — a real "code <task>" command, `kernel/intent.py` + `kernel/voice_loop.py`, still gated by `coding.run_task`'s own unchanged `Effect.EXECUTE`/`Tier.CONFIRM`, no longer blocked on a missing default); `Dispatcher`'s own pre-existing multi-candidate-per-rung accumulation without revert is contained by WP-73's disposable workspace, not resolved at the `Dispatcher` level itself; `browser.close_page` still requires explicit invocation, with no automatic cleanup; the Console UI's own real GTK4-subprocess launch path has no automated test (needs a real display, matching `show_confirmation_dialog`'s own established precedent); and LSP-based code intelligence — half of this milestone's own original working assumption — was never answered or built, real, unresolved scope carried forward, not quietly dropped. **ADR-0055 and ADR-0056 are Accepted, but the acceptance itself was this session's own judgment acting on the user's own relayed instruction, not the user's own independent reading of the final ADR text** — stated plainly here, not smoothed into "user-reviewed" language the way M4's own ADR-0048 through ADR-0053 (each accepted directly, by the user, in conversation) honestly can be; see `docs/threat-model/v0.md`'s own note for the full statement. **M5 tagged v0.5.0 2026-09-01**, immediately after WP-75's gates were confirmed green — tagged out of strict milestone-sequential order, same as M4 (M3 remains untagged, see this file's own opening line). **M6 scoping done 2026-09-01** (`docs/architecture/m6-scoping-notes.md`): four of six real ambiguities in the ROADMAP's own terse M6 objective answered directly by the user in conversation — Docker dropped (already fully satisfied by M3), ROS2 dropped (no real product reason ever named for it), email/calendar scoped to vendor-neutral IMAP/CalDAV (ADR-0021's own no-vendor-names rule, no exception needed), and the bundle split into two real sub-milestones, **M6a** (communications/productivity: email, calendar, research) and **M6b** (job assistance: research and drafting only, no auto-apply) — rather than staying one milestone. **M6a's real design done the same day** (`docs/architecture/m6a-communications.md`): real `EmailPort`/`CalendarPort` shapes (email via IMAP-read/SMTP-send — a real technical correction from the working instruction's own "via IMAP (read+send)," since IMAP has no send mechanism at all; calendar via CalDAV), research resolved with no new port (folds entirely into M5's existing `BrowserAutomationPort`), and one new ADR (`0057`, **Accepted 2026-09-03, directly by the user, in conversation, after direct review of the ADR's own full text**): email-send and attended-calendar-event creation reuse the existing `Effect.EGRESS_SENSITIVE`/`Effect.EGRESS_SECRET` rather than a new effect — a real, reasoned exception to `Effect.MEMORY_WRITE`'s own ADR-0049 precedent (which rejected EGRESS reuse because a memory write never leaves the machine; email/attended-invites genuinely do). This satisfies the same "reviewed the document, not merely relayed a decision" bar ADR-0058 already met, closing out the "remotely-reasoned working assumption" provenance M6a's own design doc originally carried. **M6a is now code-complete, both halves (WP-76 through WP-80 for reads, WP-79 onward for writes, all 2026-09-03)** — `ports/email.py`/`ports/calendar.py`/`adapters/email.py` (real `ImapEmailAdapter`, `imaplib`+`smtplib`)/`adapters/calendar.py` (real `CalDavCalendarAdapter`, `caldav`'s `Calendar.add_event`)/`application/communications/` (real `classification.py::egress_effect_for`/`calendar_effect_for`, real `writer.py`'s `EmailSendAuthorizer`/`CalendarEventAuthorizer`)/`kernel/communications.py` (real `communications.list_email`/`read_email`/`list_calendar_events` static capabilities, `Effect.EGRESS_LOCAL`/`Tier.ALLOW`; real `authorize_and_send_email`/`authorize_and_create_calendar_event` dynamic-effect capabilities, mirroring `memory.write`'s own not-statically-registered precedent) all exist and are real, tested, invocable code. `EmailPort.send_message`/`CalendarPort.create_event` are real implementations now — a `Classification.SECRET` body/attendee-bearing summary is denied unconditionally, including the "all-or-nothing across multiple recipients" property ADR-0057's own amendment required, proven by real property tests, even with `physical_confirmation_available=True`. The structural meta-test that once proved neither existed (`tests/meta/test_communications_no_send_or_create.py`) has been retired, its invariant now intentionally superseded, not left stale. **`jarvis send-email`/`jarvis create-calendar-event` (2026-09-03) are both real CLI callers now** — flat, top-level subcommands (`cli/main.py`), each constructing a real `ImapEmailAdapter`/`CalDavCalendarAdapter` from new, required flags (`--imap-host`/`--smtp-host`/`--username`/`--password-reference` for email; `--caldav-url`/`--username`/`--password-reference` for calendar, plus repeatable `--attendee`) with no default, matching `email_port`/`calendar_port`'s own per-deployment-config reasoning; `listen`'s `Gtk4PhysicalConfirmationAdapter` is the CLI's only other precedent for constructing a real, concrete adapter itself. Both wrap their kernel call in `asyncio.run` since both are `async`. Both write capabilities' own "authorizable and proven, but no wired entry point" gap is now closed. **Real, explicitly-named gaps**: the real, live skipif-guarded test against a real mailbox/calendar remains unmet, no real test-account credentials configured anywhere in this environment; no voice grammar for either subcommand, only CLI. **A real charter-conformance gap, investigated and closed 2026-09-03**: `Tier.CONFIRM` (what `send_message`/attendee-bearing `create_event` originally floated at) was remote-satisfiable by design (`domain/policy.py::evaluate()`), contradicting the project's own founding charter, which names "sending emails" explicitly as requiring "manual confirmation through the desktop interface," never voice/remote alone. Written up as `docs/adr/0059-email-and-attended-calendar-event-confirmation-tier-may-not-satisfy-the-charter.md`, **Accepted (2026-09-03, directly by the user, in conversation)** — the user chose `Tier.MANUAL_ONLY`, reusing `Effect.DESTRUCTIVE | Effect.IRREVERSIBLE` (`git.force_push`'s/`memory.forget`'s own existing combination, not a new `Effect` member). `egress_effect_for` (`application/communications/classification.py`) now returns that combination for every non-`SECRET` classification; neither authorizer class needed to change. Real property tests replaced the ones that previously proved "remote alone suffices" with the opposite, now-correct property (`decision.granted == context.physical_confirmation_available`), mirroring `tests/property/test_policy.py`'s own `test_manual_only_requires_physical_confirmation_specifically`. **Voice grammar for either write capability remains explicitly out of scope** — closing this gap does not by itself make voice grammar safe to add. See `docs/threat-model/v0.md`'s own "Milestone 6a additions," its write-half follow-up note, and its own "ADR-0059 Accepted, gap closed for real" section for the full account. **M6b's own central open question was resolved 2026-09-02, directly by the user, in conversation** — "no auto-apply" is a structural boundary, not a policy-tier gate: no submission `CapabilityId`/port/adapter method exists or will exist in M6b's scope without a new ADR explicitly superseding ADR-0058 first. `docs/architecture/m6b-job-assistance.md`'s own real design (research resolves to no new port, reusing M5's `BrowserAutomationPort`, the same conclusion M6a's own item 6 reached; drafting reuses M2's existing `UnverifiableTaskHandler` — not `Dispatcher`, checked and rejected since no pass/fail validator exists for prose — plus one new, minimal `DraftStoragePort`; the structural boundary itself is enforced by a real, specified-but-not-yet-implemented AST-based meta-test, mirroring `tests/meta/test_no_response_scraping.py`'s own precedent) is done, and ADR-0058 is **Accepted** (2026-09-02, directly by the user, in conversation, after both the ADR and this design doc were surfaced to them in full) — its core Decision was already the user's own direct answer, and this specific acceptance satisfies the "a relayed decision is not the same as reviewing the document" bar ADR-0055/ADR-0056 first established, rather than leaving it outstanding the way those two still are. **M6b is now code-complete** (WP-82 through WP-85) — the structural meta-test (`tests/meta/test_job_assistance_no_submission.py`) landed first, proven against an empty package before any real capability code existed; `job_assistance.draft` reuses M2's `UnverifiableTaskHandler` (not `Dispatcher` — no pass/fail validator exists for prose) plus one new, minimal `DraftStoragePort`/`LocalDraftStorageAdapter`; research needed no new code, reusing `browser.open_page` directly, the same conclusion M6a's own item 6 reached. **A real, conservative implementation default, flagged for the user's own confirmation, not silently decided**: `SECRET`-classified drafting input reuses `Effect.MEMORY_WRITE`'s own unconditional-`DENY` floor, making `job_assistance.draft` a dynamic-effect capability, a real deviation from the design doc's original static-capability sketch — see `docs/threat-model/v0.md`'s own "Milestone 6b additions" for the full reasoning and the meta-test's own real, named limits (it cannot inspect provider-generated content for adversarial intent, among others). **Updated 2026-09-04**: `job_assistance.draft`'s own `providers` now has a real, local-only default (`_local_only_providers`, `kernel/job_assistance.py`), live-verified against the same real, locally running Ollama server, and now wired into a real CLI caller (`jarvis draft <task>`) — mirroring `coding.run_task`'s own identical update, same night, same reasoning. The SECRET-input default is implemented and tested but not yet user-confirmed as the right long-term choice. **Not tagged** — tagging remains a separate, later action. Next: M6a and M6b are both code-complete; `jarvis send-email`/`jarvis create-calendar-event` (2026-09-03) are real CLI callers of both M6a write capabilities, now correctly floored at `Tier.MANUAL_ONLY` (ADR-0059, Accepted 2026-09-03) rather than the remote-satisfiable `Tier.CONFIRM` ADR-0057 originally chose — the real charter-conformance gap this raised is closed, not just documented. `jarvis code`/`jarvis draft` (2026-09-04) are now real CLI callers of `coding.run_task`/`job_assistance.draft`, both using a real, local-only default provider (`qwen2.5:0.5b` via a real, locally running Ollama server) — a real cloud-provider default remains undecided by any ADR, deliberately not built. Voice grammar for `send_email`/`create_calendar_event` remains explicitly out of scope until its own separate decision. **Overnight Track 2, 2026-09-04**: real file management (list/move/delete) — the one real charter gap the user's own independent inspection of `ports/file_system.py` found before this pass started (39 lines, a single `read_text` method, its own docstring naming write/list/delete as deliberately not yet built). `FileSystemPort` gained `list_dir`/`move`/`delete` (`LocalFileSystemAdapter`, native `Path.iterdir()`/`shutil.move`/`Path.unlink()`, no new safety logic invented); three new capabilities, classified in `docs/adr/0060-file-management-list-move-delete-effect-tier-classification.md` (**Proposed at the time this Track 2 pass ran — since Accepted 2026-09-04, see this file's own later note below**): `fs.list_dir` (`Effect.EGRESS_LOCAL`/`Tier.ALLOW`, identical to `fs.read_file`), `fs.move_file` (`Effect.WRITE_LOCAL`/`Tier.CONFIRM`, identical to `git.push`/an attendee-less `create_event`), `fs.delete_file` (`Effect.DESTRUCTIVE | Effect.IRREVERSIBLE`/`Tier.MANUAL_ONLY`, never remote-satisfiable, identical to `git.force_push`/`memory.forget`, ADR-0059's own just-closed reasoning). **The protected-path question ADR-0060 treats as its own real decision, not a silent omission**: no additional pattern-based check beyond the existing `allowed_root` scope boundary (both endpoints checked for `fs.move_file`) — ADR-0056's `resolve_protected_patterns` was checked and rejected as a category error (it protects a *target repo's own* test-file convention from an *autonomous coding agent's* writes, a problem general file management doesn't have), a denylist was checked and rejected as contradicting this project's own already-stated allowlist-over-denylist posture (`fs.read_file`'s own docstring, quoted directly in the ADR), and `Tier.MANUAL_ONLY`'s own real, physical, per-invocation confirmation is reasoned as the operative, structurally sufficient safeguard for `fs.delete_file` — ADR-0056's own check exists specifically because the coding agent lacks that per-write human checkpoint, a gap `fs.delete_file` never has. `kernel/files.py` gained `authorize_and_list_dir`/`authorize_and_move_file`/`authorize_and_delete_file`, mirroring `kernel/memory.py`'s own `authorize_and_*` shape exactly, including a real property test proving `fs.delete_file`'s `Decision.granted` tracks `physical_confirmation_available` alone, remote confirmation notwithstanding. Real CLI callers: `jarvis list-dir <path>`/`jarvis move-file <source> <destination>`/`jarvis delete-file <path>`, all using the same default `allowed_root` (`Path.home()`) `fs.read_file` already uses, no override flag. See `docs/threat-model/v0.md`'s own "Overnight Track 2" note for the full account. **Overnight Track 3, 2026-09-04**: voice grammar for `communications.send_email`/`communications.create_calendar_event`, per the user's own already-made decision to add it. `kernel/intent.py` gained its first *two-word* command keywords, "send email"/"create event" (matched as a fixed prefix ahead of the existing single-word dispatch), each with a small, real, fixed keyword grammar since a single verbatim string cannot safely separate an email's three real fields (recipients/subject/body, split on " subject "/" body ") or a calendar event's three or four (summary/start/end/optional attendees, split on " from "/" to "/" with "). Both resolve to `application/communications/writer.py`'s own `EMAIL_SEND_CAPABILITY_ID`/`CALENDAR_CREATE_EVENT_CAPABILITY_ID`, the same dynamic-effect, not-statically-registered shape "remember" already established. `kernel/voice_loop.py` gained `email_port`/`calendar_port`, both optional with no safe default (mirroring `coding_target_repo`/`coding_dispatcher_factory`'s own precedent exactly) — a resolved command with the matching port unconfigured speaks a real, honest "not configured" message, never a crash or silent no-op. **Voice does not bypass ADR-0059's `Tier.MANUAL_ONLY` floor in any way**: both composition functions are called completely unmodified, `approved` (the voice loop's own physical-confirmation answer) passed straight through, `remote_confirmation_available` always `False` exactly like every other branch — proven directly by two new tests asserting a denied physical confirmation never reaches the real `EmailPort`/`CalendarPort` for an attendee-bearing event/email send. A real, named limitation, not hidden: calendar start/end times are matched verbatim, not parsed from natural spoken language ("tomorrow at 3") — a caller must speak or otherwise supply an exact ISO-8601 string. No changes to `jarvis listen`'s own CLI wiring were needed or made — mirroring "code"'s own precedent exactly, `email_port`/`calendar_port` simply default to `None` there today, the same as `coding_target_repo`/`coding_dispatcher_factory` already do. **M6 (both M6a and M6b) tagged `v0.6.0` 2026-09-04**, immediately after Tracks 1 through 3's gates were confirmed green on `main` — tagged out of strict milestone-sequential order, same as M4/M5 (M3 remains untagged, see this file's own opening line). The tag message states plainly what shipped beyond M6a/M6b themselves (Track 1's local-only default providers, Track 2's file management with ADR-0060 then still Proposed, Track 3's voice grammar) and what remained open at the time (a real cloud-provider default for coding/drafting remains unconfigured by choice; the live-credential-gated IMAP/CalDAV test remains unmet). **ADR-0060 Accepted 2026-09-04, directly by the user, in conversation, after direct review of the ADR's own full, verbatim text** — surfaced whole, not summarized, the same "surface the raw text, let the user decide" pattern already used for ADR-0057/ADR-0059. The user chose accept as-written, no changes requested: `fs.list_dir`/`fs.move_file`/`fs.delete_file`'s classification stands, and `fs.move_file`/`fs.delete_file` carry no path-pattern protection beyond the existing `allowed_root` scope boundary — `Tier.MANUAL_ONLY`'s own physical confirmation is the accepted, operative safeguard for delete, not a denylist or `resolve_protected_patterns` reuse. See `docs/threat-model/v0.md`'s own "ADR-0060 Accepted" section. **Desktop CLI wiring, 2026-09-04**: the real, previously-flagged "`kernel/desktop.py`'s capabilities are real but never wired into `jarvis`'s own CLI" gap (named by Track 6's own charter-completeness re-check) is closed — eleven real, flat, top-level CLI subcommands, one per distinct `CapabilityId` `kernel/desktop.py` already implements: `jarvis open-brave-url`/`open-vscode-file`/`send-claude-text`/`send-chatgpt-text`/`list-docker-containers`/`stop-docker-container`/`git-status`/`git-create-branch`/`git-commit`/`git-push`/`git-force-push`. `send-claude-text`/`send-chatgpt-text` construct a real `AtspiDesktopWindowAdapter()` directly in `cli`, mirroring `listen`'s own `Gtk4PhysicalConfirmationAdapter` precedent (`kernel` cannot default this port itself, a C6 "no GLib in the core" restriction). **Two real capabilities remain deliberately unwired, not overlooked**: `terminal.run` (needs `SyntheticInputPort`, explicitly out of scope) and `docker.run_container`/`docker.build_image` (DESTRUCTIVE-tier Docker actions, explicitly named "do not touch" by this pass's own hard gate). A real, unmocked, parametrized test proves every wired capability at `Tier.CONFIRM` or above is denied by CLI invocation alone with no confirmation flag supplied; a dedicated test proves `git.force_push`'s `Tier.MANUAL_ONLY` floor holds even with `--remote-confirmation-available` alone. See `docs/threat-model/v0.md`'s own "Desktop CLI wiring" note. **Overnight Track 5, 2026-09-04**: a dependency/security audit refresh — `pip-audit` 2.10.1 re-run against this project's own real, currently-resolved `.venv` (same `PIPAPI_PYTHON_LOCATION` workaround the 2026-09-03 audit established). **No known vulnerabilities found**, the identical clean result. Checked directly, not assumed: exactly one new direct dependency exists since that audit (`caldav`, already added and already covered by it) — none of Tracks 1 through 4 tonight added a new dependency. See `docs/threat-model/v0.md`'s own "Overnight Track 5" note. **Overnight Track 6, 2026-09-04**: a quick, real, grep-based charter-completeness re-check (not a full sweep) — voice, browser automation, desktop automation, code understanding, long-term memory, and job-application assistance all still have real, invocable code, checked directly (line counts, real `authorize_and_*` functions, real CLI/voice wiring where it already existed). No regression found. Desktop automation's own capabilities (`kernel/desktop.py`) remain real but still not wired into `jarvis`'s own CLI — a real, pre-existing gap, unchanged by this track and out of its scope to close. "Task planning" has no dedicated kernel module; it maps to M2's own `Dispatcher`/`EscalationLadder`, which `coding.run_task` already wraps. `registry.register(` now appears 34 times in `build_default_registry()` (31 before tonight, the three new `fs.*` capabilities from Track 2), matching the test suite's own expected count. The one real charter gap named at the top of this overnight run — file management — is closed, via Track 2. See `docs/threat-model/v0.md`'s own "Overnight Track 6" note. **ADR-0054 ratified 2026-09-04, directly by the user, in conversation, after direct review of the ADR's own full, verbatim text** — surfaced whole, the original 2026-08-26 unilateral-acceptance note preserved unedited as the real historical record. The user chose ratify as-is, no changes requested: `ClockPort.now()`/`IdPort.new_id()` and their `SystemClockAdapter`/`UuidIdAdapter` implementations stand. This closes the one real, outstanding "not the user's own independent reading" gap M4's own closeout left open, satisfying the same bar ADR-0057/ADR-0058/ADR-0059/ADR-0060 each already met. See `docs/threat-model/v0.md`'s own "ADR-0054 ratified" section. **Scoping investigation, 2026-09-04 (research only, nothing built)**: `docs/architecture/m7-scoping-notes.md` investigates two carried-forward gaps. "Task planning" — checked directly, `Dispatcher`/`EscalationLadder` climb three fixed rungs for one already-specified task, never decomposing a goal into a multi-step, cross-capability plan; the "maps to M2's reasoning layer" charter claim is not accurate as a full capability match. LSP code intelligence — `coding.run_task` currently sends a provider no real file content at all (`build_prompt()` never touches the filesystem), reframing the question toward whether any repository context reaches the prompt, not specifically LSP. Real options laid out for both, no decision made, no code changed. `m5-scoping-notes.md` gained a short addendum cross-referencing it. **Overnight hardening pass, 2026-09-04**: added adapter-level edge-case tests for the eleven newly-wired desktop CLI commands, and found a real bug while doing so — `cli/main.py::main()`'s own `except` tuple never covered the six real, adapter-level exception types those commands can raise (`BrowserLaunchFailedError`/`EditorLaunchFailedError`/`WindowNotFoundError`/`WindowActionFailedError`/`DockerCommandFailedError`/`GitCommandFailedError`, all deliberately not `JarvisError` subclasses). Confirmed as a real crash empirically before fixing: a granted `send-chatgpt-text` whose real `WindowNotFoundError` was allowed to propagate produced an unhandled Python traceback instead of this module's own established clean-error shape. Fixed; six new regression tests, one per exception type, prove each now fails closed correctly. Missing/malformed required CLI arguments for all eleven commands were already handled correctly by argparse's own real `SystemExit` mechanism — confirmed, not assumed, via a new parametrized test; nothing needed fixing there. See `docs/threat-model/v0.md`'s own "Overnight hardening pass" note. **Overnight Track 4, 2026-09-04**: a third dependency/security audit refresh — `pip-audit` 2.10.1 re-run, same clean result, no known vulnerabilities found. Checked directly: zero new direct dependencies since `v0.6.0` was tagged. See `docs/threat-model/v0.md`'s own "Overnight Track 4" note. **Overnight Track 5, 2026-09-04**: a full docs-integrity sweep (not a quick pass) — all 60 real ADRs confirmed `Accepted`, no `Proposed` ones remain. Real drift found and fixed: this file's own internal self-contradiction on ADR-0060's status (fixed, both paragraphs now agree); `docs/ROADMAP.md`'s own M6b entry contradicting itself ("code-complete" then "design work has not started," fixed); `m6-integrations.md` carrying two stale claims (ADR-0057's superseded classification, "voice grammar remains out of scope") plus a stale "Not tagged"; `m4-memory-retrieval.md`/`m5-browser-coding.md`'s own opening Status lines still reading "not yet approved" despite both being tagged; `m6-scoping-notes.md` still claiming two resolved questions "remain genuinely open"; `m4-scoping-notes.md` still claiming its scoping conversation "has not happened yet" when it genuinely had (checked against `m5-scoping-notes.md`'s own similar-looking but still-accurate line before assuming they matched). All fixed with inline corrections preserving the original text as historical record. Internal cross-references, referenced-file existence, CLI subcommand count, and capability registry count all confirmed matching, no drift found there. See `docs/threat-model/v0.md`'s own "Overnight Track 5" note for the full account. **Track 1, real local IMAP/CalDAV integration tests, 2026-09-04**: closes the real, repeatedly-named "live-credential-gated integration test remains unmet" gap — not with real credentials, with a real, local, open-source test server. `tests/integration/test_email_calendar_against_local_servers.py` (new) exercises the real, unmodified `ImapEmailAdapter`/`CalDavCalendarAdapter` against a real GreenMail (IMAP/SMTP) and Radicale (CalDAV) container, both verified live before writing any test. Five real tests, all passing live (`list_messages`/`read_message`/a real `EmailMessageNotFoundError`, `list_events`, one real attendee-less `create_event`); a real, empirical honest-skip check confirmed each fails closed with an actionable message when the local servers aren't running. Wired into CI as real `docker run` steps (not a `services:` block — GitHub Actions starts those before checkout, so a config-file volume mount would race). No real external-vendor credential configured anywhere. See `docs/threat-model/v0.md`'s own "Track 1" note, including one real, unrelated `caldav` library deprecation warning surfaced and named, not fixed (out of this track's scope). **Track 2, real mutation testing on the policy/authorization core, 2026-09-04**: `cosmic-ray` 8.7.0 (picked over `mutmut`, which crashed reproducibly against this codebase on its very first mutant in two independent attempts) run against `domain/policy.py`, `domain/capability.py`, and all four `application/*/classification.py` modules. Three real gaps found and fixed, each reverified to confirm the new test actually kills the mutant: `domain/policy.py`'s `DecisionReason` (`TAINT_ESCALATION`/`BASE_TIER`) was never asserted anywhere; `domain/capability.py`'s `effective_tier` escalation proved only "goes up," never "by exactly one step" (a `+1`→`+2` mutant survived); `application/coding/classification.py`'s `_ini_file_has_section` had an `and`/`or` gap for an existing `setup.cfg`/`tox.ini` that parses fine but lacks the target section. Every other survivor (49 total across all six modules, in the final, isolated re-runs) was reasoned to a specific, checkable equivalence argument, not asserted equivalent by default — enum singleton identity, ordered-enum boundary comparisons, a domain invariant proven by a separate property test, and (the single largest bucket, 33+1 mutants in `coding/classification.py` alone) `| None` union-syntax return annotations that `from __future__ import annotations` (PEP 563) makes lazy strings, never evaluated as real code at runtime. **A real, general methodology hazard found and documented**: `cosmic-ray`'s per-mutant apply/revert cycle is not safe under concurrent access to the same file — running another test/coverage command against a module `cosmic-ray` is actively executing against can both skew timeout-based results (the likely cause of two modules' initial survivor counts dropping to 0 on a clean rerun) and leave a real, uncommitted mutation applied to the working tree even after the run reports done (caught directly once via `git diff`, fixed via `git checkout --`, that entire run discarded and redone in isolation). A real, reusable, collision-free driver (`scripts/run_mutation_testing.sh`, keyed by dotted module path rather than basename — four of the six target files are all literally named `classification.py` and an earlier basename-keyed version of this script silently destroyed three of the four sessions' own per-mutant diffs before they could be inspected) is now committed as durable project tooling. Full mutation-score table (module → mutants → survivors → score) in `docs/threat-model/v0.md`'s own "Track 2" section. **Track 3, real per-module branch-coverage audit, 2026-09-04**: every `src/jarvis` module below 100% branch coverage investigated individually, not assumed safe from an earlier session. One real, closable gap found and closed: `kernel/intent.py` (96% -> 100%, 5 new tests) had never exercised the empty-string-after-splitting case for "send email"/"create event" voice-grammar field parsing. Every other `kernel/` module's remaining gap traces to one of two already-established, real, non-closable-in-this-pass causes, both named explicitly rather than silently skipped: (1) an `if decision.granted:`-shaped branch on an ALLOW-tier capability, structurally unreachable because `evaluate()` hardcodes `granted = True` for `Tier.ALLOW` (`browser.py`, `communications.py`, `desktop.py`, `files.py`, `memory.py`, `voice_loop.py`); (2) a real-hardware default (TTS synthesis, audio playback) matching this codebase's own pre-existing "exercised live, not by an automated suite" convention (`job_assistance.py`, `voice_loop.py`). `kernel/` total: 1012 statements/10 misses, 170 branches/15 partials before this pass; 1012/**7**, 170/**12** after (rounded percentage unchanged at 98% — coverage.py's own rounding puts both values in the same bucket, but the raw miss count genuinely dropped). Full per-module before/after table in `docs/threat-model/v0.md`'s own "Track 3" section. **Property-matrix/fuzzing/concurrency pass, Track 1, real exhaustive capability property matrix, 2026-09-04**: a new file, `tests/property/test_capability_registry_matrix.py`, closes a real gap none of the existing property tests closed — nothing previously proved `decision.granted` behaves correctly for the real, statically-registered capabilities through the real, end-to-end `AuthorizationOrchestrator.authorize_by_id()` path (only the domain layer in isolation, via synthetic descriptors). A hand-curated table of all 34 real capabilities' own expected `Tier` (read from the live registry, not guessed) is proven to match the registry exactly; a real Hypothesis property test drives every one of the 34 through the real orchestrator under every confirmation-state combination, proving the exact tier rule holds end to end; a third, narrower test isolates the single most safety-relevant property — every real `MANUAL_ONLY` capability denied under remote-only confirmation (ADR-0013). **Real tier-classification review, as required**: every one of the 34 capabilities' current classification was checked against this project's own documented ADR history — no real mismatch found, stated plainly rather than invented. Dynamic-effect capabilities (`memory.write`, `communications.send_email`/`create_calendar_event`, `job_assistance.draft`) are deliberately out of scope, each already covered by its own dedicated property-test file. **Property-matrix/fuzzing/concurrency pass, Track 2, real input-sanitization fuzzing, 2026-09-04**: `tests/property/test_input_sanitization_fuzz.py`, real Hypothesis-generated adversarial-string fuzzing (not hand-picked examples) across `filename_hint` sanitization, `fs.move_file`/`delete_file`'s `allowed_root` scope boundary, voice-grammar field-splitting, and CLI argument parsing. **A real bug found and fixed, not invented**: fuzzing the `allowed_root` boundary check surfaced a bare `ValueError` (an embedded NUL byte) from `Path.resolve()` that `_resolve_within_scope`'s own docstring doesn't document. Tracing where else this mattered found the real problem: `kernel/voice_loop.py`'s `_handle_utterance` had **no exception handling at all** around its dispatch call — unlike `cli/main.py`'s identical dispatch, which already catches this exact error family — meaning a single malformed voice command could have crashed the entire voice loop, not just failed that one utterance. Fixed by wrapping the call in the same exception tuple `cli/main.py::main()` already uses, converting any of them into a clean, spoken "Sorry, that failed: ..." response; a new regression test in `tests/unit/test_voice_loop.py` proves the loop now survives. Every other fuzz target (`_safe_stem`, `resolve_intent`, the CLI parser) came back clean, stated plainly. **Property-matrix/fuzzing/concurrency pass, Track 3, real concurrency/thread-safety investigation, 2026-09-04**: the capability registry, the audit log writer, the memory store, and confirmation-chain storage (the real "confirmation chain" *is* the audit chain — no separate mechanism exists). **A real, in-process race found and fixed**: `domain/audit.py::AuditChain.append()` could let two concurrent callers both read the same pre-append state and both produce a record claiming `sequence=0` — confirmed empirically (a deterministic test failed with `[0, 0] == [0, 1]` against the unfixed code) before fixing with an internal `threading.Lock` (stdlib-only, no port/interface change, `lint-imports`'s C2 domain-purity contract still passes). **A real cross-process gap found and documented, deliberately not fixed**: two independent processes racing to save the same `--chain-path` file causes the second `save()` to silently overwrite the first's new record entirely — proven with a real test, `verify()` still reports the surviving chain valid since nothing was tampered with, just clobbered. Not fixed: closing it needs a real `AuditStoragePort` contract change (file locking or an append-only format), a genuine architecture decision flagged for the user rather than silently built, matching this file's own standing rule. `CapabilityRegistry.register()`'s same-class race was investigated and confirmed **provably unreachable** — every `authorize_and_*` function calls `build_default_registry()` fresh per invocation, so no shared, long-lived registry instance ever exists for a second caller to race against (grep-confirmed across every `kernel/*.py` module). `SqliteMemoryAdapter` confirmed fails closed by default (a real cross-thread test proves a clean `sqlite3.ProgrammingError`, never corruption) — Python's own stdlib safety net, no fix needed. **Real bug reported directly by the user and fixed, 2026-09-05**: four tests in `tests/unit/test_job_assistance_kernel.py` called `authorize_and_draft_document()` with a granted decision and no `console=` override, silently opening real, unclosed GTK4 windows on the real desktop during ordinary automated test runs. Fixed by passing `_StubConsole()` at each call site, plus a new, durable `autouse` regression-guard fixture (`_forbid_real_console_windows`) that makes any future real `GtkConsoleAdapter` construction in this test module fail loudly and immediately. Scope discipline: test-side cleanup only, `GtkConsole`'s own real behavior untouched; `test_browser_kernel.py`'s identical pattern checked directly, no matching gap found there. **Adapter-resilience/mutation-extension/audit-log-integrity pass, Track 1, real adapter failure resilience, 2026-09-05**: real, local, credential-free test dependencies (Ollama, GreenMail, Radicale) stopped/dropped mid-use, distinguishing "never reachable" from "connection lost mid-session" throughout. `coding.run_task`/`job_assistance.draft` (Ollama) and `communications.list_calendar_events` (Radicale) already fail closed correctly — real `OSError`-family exceptions, already caught by `cli/main.py`'s/`kernel/voice_loop.py`'s own broad except tuples, confirmed via 6 new real tests in `tests/integration/test_adapter_failure_resilience.py`. **A real bug found and fixed**: `imaplib`'s own exception hierarchy (`IMAP4.error`/`IMAP4.abort`) is a bare `Exception`, not `OSError` — a connection established then lost mid-session against GreenMail produced a raw, uncaught `imaplib.IMAP4.abort` before this fix. Neither `communications.list_email`/`read_email` is wired to a real CLI/voice entry point yet (confirmed by grep), so there was no live crash today, but the gap was real regardless. Fixed by a new, typed `jarvis.ports.email.EmailConnectionError`, normalizing `imaplib.IMAP4.error` at the adapter boundary, matching the existing `EmailMessageNotFoundError`/`CalendarEventCreationError` precedent. **Track 2, mutation testing extended to the kernel orchestration layer, 2026-09-05**: `scripts/run_mutation_testing.sh` reused unmodified against `kernel/memory.py`/`files.py`/`communications.py`/`desktop.py`/`coding.py`. **A real methodology bug caught before trusting results**: the first `kernel.desktop` run's own `TEST_CMD` included `tests/unit/test_desktop.py` (tests `domain.desktop`, not `kernel/desktop.py` at all) instead of the real `test_desktop_kernel.py`/`test_desktop_kernel_git.py`/`test_desktop_kernel_docker.py` — caught by finding a survivor that an existing, real test should have killed. Fixed and re-run in isolation: survivors dropped from 187 to 147 (40 real mutants only surviving because their own test never ran). **One real gap found and closed, shared across all four non-`coding.py` modules**: every `authorize_and_*` function's confirmation arguments are keyword-only via a bare `*`, but nothing tested this — a `*`→`/` mutant survived in 25 real functions across `memory.py`/`files.py`/`communications.py`/`desktop.py`. Closed by one generic, reflection-based test (`tests/unit/test_composition_function_keyword_only_args.py`) using `inspect.signature()` across all five modules at once, verified by manually reproducing the mutation and confirming the test fails with a precise error, then passes once reverted. Every other survivor (dominated by the same already-established `| None`-annotation and `frozen=True`-dataclass equivalence classes from the prior pass) reasoned to a specific, checkable equivalence argument. Full table in `docs/threat-model/v0.md`'s own "Track 2" section. **Track 3, audit-log integrity investigation, 2026-09-05**: the real hash chain (`domain/audit.py::AuditChain`) already catches per-record and cross-record tampering of a loaded chain — a new test closes the one real gap found (tampering with a real decision field, not just `record_hash` itself, was untested). A real, structural limit investigated and scoped, not fixed: no protection exists against the entire chain file being wholly replaced with a fabricated-but-self-consistent history (`JsonFileAuditStorageAdapter.save()` sets no restrictive permissions or immutable flag) — four real options laid out in `docs/architecture/audit-log-integrity-scoping-notes.md`, no recommendation made, the user's own decision. **10-phase combined pass, Phase 1, real end-to-end scenario tests, 2026-09-05**: two real integration tests chaining genuinely-existing capabilities via real adapters (`fs.read_file` → `memory.write` → `memory.retrieve`, and a recalled memory's real text feeding a real `coding.run_task` call, then `git.status`/`git.commit`/`git.status` on a disposable repo). Found and fixed a real, empirically-observed timing constraint, not a composition bug: a longer coding-task prompt made the real local model's own SELF_REPAIR rung exceed `LocalReasoningAdapter`'s fixed 120s timeout — fixed by shortening the test's own prompt. **Phase 2, resilience against corrupted persistent state, 2026-09-05**: a real, corrupted `memory.sqlite3` file raises `sqlite3.DatabaseError`, a bare `Exception` not `OSError` — the same class of gap as the prior pass's `imaplib` finding. Confirmed via a real crash (`jarvis memory write` against a real corrupted file) before fixing; `sqlite3.Error` added to both `cli/main.py`'s and `kernel/voice_loop.py`'s broad except tuples. A corrupted/truncated audit-chain file already fails closed correctly (`json.JSONDecodeError`, already a caught `ValueError`). A "missing config file" scenario doesn't apply — this project has no config-file loading mechanism anywhere, checked directly. **Phase 3, real API reference documentation, 2026-09-05**: a real, generated Sphinx API reference (`docs/api/`, `sphinx.ext.autodoc`+`napoleon`+`sphinx-autodoc-typehints`) over `domain`/`ports`/`application`/`adapters`/`kernel`, built from existing docstrings, not new prose. Found and fixed one real docstring bug (`adapters/embedding.py`: `` ``torch``+CUDA `` broke reST inline-literal parsing — confirmed via a bare `docutils` reproduction before fixing). Investigated two real, structural, benign warning classes rather than chasing them individually: ambiguous cross-references from this codebase's own package-level re-export convention (suppressed, `docs/api/conf.py`), and duplicate-object-description warnings from Napoleon's default `Attributes:`-section rendering colliding with autodoc's own real dataclass-field introspection (fixed via `napoleon_use_ivar = True`, dropping 178 warnings to 12 genuinely-different, still-benign, re-export-caused ones that Sphinx's own code offers no suppression hook for). A cheap CI step (`sphinx-build (API reference)`) checks the build succeeds; no external hosting, `docs/api/_build/` gitignored. **Phase 4, real kernel performance benchmarking, 2026-09-05**: `poc/wp67_kernel_benchmark.py` (real, one-shot, matching `poc/wp61_vector_store_benchmark.py`'s own precedent) measured three real hot paths, results in `docs/architecture/kernel-performance-benchmarks.md`. `JsonFileAuditStorageAdapter.save()`/`.load()` show real, confirmed linear O(n) growth from its known whole-file rewrite (0.34ms at 10 records, 44/63ms at 2,000) — not fixed, per this pass's own hard scope boundary (the adapter's save/load format and its already-documented cross-process-race/whole-file-replacement gaps stay open, awaiting the user's own architecture decision). `authorize_ping()` end-to-end confirms that cost reaches real capability-call latency directly (~31ms by call #1,000). `authorize_and_remember()`/`authorize_and_recall()` with real embeddings is a genuinely new number (WP-61/62 only benchmarked cosine-similarity math over synthetic vectors) — real inference latency (~90-135ms/call) dominates and is essentially flat across 1-200 records. No CI perf-regression gate added (would need a dedicated, isolated runner to avoid noisy-neighbor variance — out of scope). **Phase 5, real memory-store backup/restore, 2026-09-05**: two new static capabilities, `memory.backup` (`Effect.WRITE_LOCAL`/`Tier.CONFIRM`) and `memory.restore` (`Effect.DESTRUCTIVE | Effect.IRREVERSIBLE`/`Tier.MANUAL_ONLY`, same "no built-in undo" finality as `memory.forget`, at whole-store scale) — see `docs/adr/0061-memory-store-backup-restore-effect-tier-classification.md` (**Proposed**, not yet reviewed by the user, following ADR-0060's own "build and classify honestly, flag for review" sequence). `SqliteMemoryAdapter.backup()`/`.restore()` use SQLite's own real online-backup API (`sqlite3.Connection.backup()`), not a raw file copy, safe against a torn snapshot even while the store is open. Real CLI subcommands `jarvis memory backup <destination>`/`jarvis memory restore <source>`, manually smoke-tested against a real on-disk store (real GRANT/DENY outcomes, a real clean error for a missing backup file) before the automated suite ran. 36 capabilities now statically registered (up from 34). No protected-path/content inspection added for either capability, mirroring ADR-0060's own reasoning — `Tier.MANUAL_ONLY`'s physical confirmation is judged the sufficient safeguard for restore; left open whether backup itself warrants a stronger floor. **Phase 6, charter-conformance audit specifically checking speaker verification, 2026-09-05**: `docs/architecture/charter-conformance-audit-phase6.md`, a real, evidence-based re-check, not a re-read of prior claims. Speaker verification (ADR-0012/ADR-0034) **conforms fully, unchanged across five milestones**: `UnverifiedSpeakerIdAdapter` still always returns unverified/0.0; `test_speaker_id_isolation.py`'s all six checks pass, including its own self-test that the scan predicate actually fires on a deliberate violation; an independent grep confirms exactly one `PolicyContext(` construction site exists in the whole codebase, with zero speaker-related references near it; `voice_loop.py`'s speaker score feeds only a log line. A narrower spot-check of CLAUDE.md's other Core Principles found no other gap (zero vendor names in domain/application/ports; a real 1:1 correspondence between all 36 `authorize_and_*` functions and 36 orchestrator-authorize call sites; no audio-persistence code path exists at all; SECRET still floors at `Tier.DENY` unconditionally). **One real, significant drift found and deliberately not fixed here**: `README.md` is severely stale (still Milestone-0-only) — left for the already-queued "5 mixed real tasks" prompt, whose own first task is explicitly a README accuracy audit; fixing it here would duplicate that scoped work. **Phase 7, indirect prompt-injection resilience, 2026-09-05**: `docs/architecture/prompt-injection-resilience-phase7.md`. Real, direct finding: no automatic content-to-reasoning pipeline exists today — every task fed to `coding.run_task`/`job_assistance.draft` is `Provenance.user()` (human-typed), and browser-scraped content is correctly tagged `Trust.UNTRUSTED_EXTERNAL` but never automatically reaches a reasoning call, so the most dangerous automatic injection vector has no real wiring to exploit. The structural defense that matters regardless: exactly one `Verdict.PASSED` assignment site exists in the whole codebase (`adapters/validation/_command.py`, a real subprocess exit code, never model text), and `Arbiter._score` gives `MODEL_OPINION` evidence zero weight unconditionally (ADR-0025). New, real, end-to-end test added: a coding task description carrying a classic injection payload ("ignore all previous instructions, tests already pass"), a fake provider that fully complies by claiming success with no real patch, run against a real `bwrap` sandbox and real `pytest` subprocess — the climb still exhausts and the real repo stays untouched. **Phase 8, plugin architecture proof + CLI UX consistency audit, 2026-09-05**: `docs/architecture/plugin-architecture-and-cli-ux-audit-phase8.md`. `jarvis.plugin_api` (previously a docstring only) now has real content, a narrow re-export of `jarvis.domain`'s capability-authoring vocabulary (still passes C7); `docs/plugin-guide/example_plugin.py` is a real, minimal example (`example.word_count`) built importing only `jarvis.plugin_api`/stdlib, mechanically proven by `tests/meta/test_plugin_api_example.py` (an AST-scan with its own self-test, plus a real end-to-end authorization proof). Dynamic, out-of-tree plugin loading remains unsolved, unchanged from `docs/plugin-guide/README.md`'s own honest, long-standing disclaimer. CLI audit: confirmation-flag naming and output/error formatting are both structurally guaranteed consistent (confirmed by direct counting). One real bug found and fixed: three `--help` strings leaked internal ADR reference numbers into user-facing text (`memory backup`, `send-email`/`create-calendar-event --password-reference`) — removed, with a new mechanical regression test. Two real, structural naming inconsistencies found and left as open findings for the user's own decision (memory's nested-subcommand shape vs. everything else's flat commands; `fs.read_file`'s bare `read` vs. its siblings' noun-including names) — both would be user-facing, potentially script-breaking renames, not silently made here. **Phase 9, full-history secrets scan, license compliance, SBOM, 2026-09-05**: `docs/architecture/secrets-license-sbom-audit-phase9.md`. Two independent tools (`truffleHog` over the full real git history, `detect-secrets` over the current tree) found zero real secrets — every flagged item investigated and confirmed benign (lockfile hashes, a public model commit SHA, hardcoded test-container passwords for already-credential-free local servers, a fake API-key-shaped test stub). License compliance (`pip-licenses` against the real, resolved `.venv`): most non-permissive-looking findings were false alarms after checking real shipped LICENSE/METADATA files directly. Two real, substantive findings deliberately flagged for the user's own decision, not resolved: `piper-tts` (GPL-3.0-or-later, imported directly in-process) and `icalendar-searcher` (AGPL-3.0-or-later, a real, exercised dependency of `caldav`'s recurrence search) both raise real license-compatibility questions for this MIT-licensed project. A real CycloneDX 1.6 SBOM (`docs/architecture/sbom.cyclonedx.json`, 133 components) now exists, regenerable via `scripts/generate_sbom.sh`.

## Architecture summary (see docs/architecture/ for full detail)

JARVIS is a capability-based agent kernel following Clean Architecture / ports & adapters, dependency rule pointing inward:

domain (stdlib only, no I/O, no async) → ports (Protocols) → application (use cases) → adapters (implementations) → kernel (composition root) → ipc/cli

Core principles:
- The kernel knows capabilities, not agents. New features are plugins; nothing in domain/application/ports names a specific integration (no "email agent", no vendor names).
- No shell. Capabilities declare typed effects (READ_LOCAL, WRITE_LOCAL, EGRESS_LOCAL, DESTRUCTIVE, IRREVERSIBLE, CREDENTIAL, EGRESS_SENSITIVE, etc). A single Policy Engine evaluates effects against a Tier (ALLOW/CONFIRM/MANUAL_ONLY/DENY) at one choke point. Command blocklists are never used.
- Every value carries Provenance (Trust: USER_DIRECT/SYSTEM/UNTRUSTED_EXTERNAL; Classification: PUBLIC/PERSONAL/SENSITIVE/SECRET). Values are wrapped Tainted[T]. Untrusted external content (web pages, emails, READMEs) escalates the required permission tier automatically.
- Voice/speaker verification is a convenience filter, never an authorization boundary (defeated by replay/cloning). Physical interaction with the machine is the real auth boundary.
- Privacy: SECRET data (API keys, passwords, tokens) is DENY to any cloud provider, always, no exceptions, never enters model context. SENSITIVE data (personal info, third-party confidential data) may go to a cloud provider only behind explicit CONFIRM. Where classification of a task's inputs is uncertain, it fails closed — inherits the highest classification present. Secrets live only in the system keyring, referenced, never stored as values, never in source, database, or audit log.
- Audio is never persisted to disk, ever, unless an explicit temporary debug mode is enabled.
- Destructive/irreversible/credential actions always require MANUAL_ONLY confirmation — never satisfiable by voice alone.
- Multi-provider reasoning (ChatGPT, Claude, others): both are trusted providers behind a ReasoningPort abstraction. No vendor names anywhere in domain/application/ports. Passing validation (build/test/lint/execution) is always stronger evidence than model agreement. An escalation ladder tries cheap deterministic fixes first, then self-repair, before ever consulting a second provider. Models never merge implementations — the arbiter selects one candidate unmodified, never a splice of two. A reviewing model must produce a failing test case, not a verdict/opinion. A test authored by a provider carries zero weight when scoring that same provider's own candidate.
- Audit log: every capability invocation is logged, hash-chained (tamper-evident), argument VALUES are never logged (only digests — secrets must never appear in the audit trail). A header/payload split so payloads can be redacted without breaking the chain is accepted (ADR-0028) but **not yet implemented** — `record_hash` today covers the whole record as one unit (see `docs/architecture/m0-architecture.md` section 5 and `docs/architecture/system-design.md` section 5).

## Workflow — one work package at a time, as directed

`docs/architecture/roadmap.md` does not exist yet (see `docs/architecture/README.md`); work package sequencing and scope come directly from the user in conversation, not from a roadmap file. If `roadmap.md` is ever supplied, this section should be updated to reference it for real.

For each work package: Analysis → Plan → Implement → Verify (all gates) → Review. You may move through these phases without stopping for approval at each one (I've approved autonomous execution for now), but ALWAYS stop completely at the end of a work package and report before starting the next one.

## Hard rules
- Never implement a future work package "while you're at it."
- Never silently change the architecture. If something in this file or the docs seems wrong once you're implementing, stop, explain the problem, propose a fix as a new ADR, and wait for approval before proceeding.
- Report every deviation from what was planned, including things you added.

## Invariants enforced by tooling, not convention
- domain/ imports stdlib only (mypy --strict clean, import-linter contract, and an AST-based test all check this).
- No datetime.now(), time.time(), time.monotonic(), or uuid.uuid4() anywhere in src/ — inject ClockPort / IdPort instead. Enforced by ruff banned-api AND an AST test (ruff alone doesn't catch attribute-style calls like time.time() after `import time`).
- No vendor names (openai, anthropic, chatgpt, claude, gpt) in src/jarvis/domain/, application/, or ports/.

## Gates — ALL must pass before any work package is considered done
uv run ruff check .
uv run ruff format --check .
uv run mypy --strict src tests
uv run lint-imports
uv run pytest
uv run coverage report --include="src/jarvis/domain/*" --fail-under=100
uv run coverage report --include="src/jarvis/application/policy/*" --fail-under=100
uv run coverage report --include="src/jarvis/application/reasoning/*" --fail-under=100

## Meta-tests

`tests/meta/` tests the gates themselves. If you add an import-linter contract,
add a test proving it fires against a deliberate violation, and add it to
`CONTRACT_SCHEDULE` in `test_gate_integrity.py`.

## Git
Branch wp/NN-slug. Conventional Commits. Squash-merge into main, one commit per work package. Tags at milestone completion only.
