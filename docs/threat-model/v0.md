# Threat Model — v0 (Milestone 0)

This document describes what is actually true of the implementation as
it exists at the end of Milestone 0 (work packages 1–16). It is not a
description of the eventual vision in `docs/adr/` — where the current
code falls short of an accepted ADR, that is stated here explicitly,
not smoothed over. Every claim below is checked against the current
source, not against what was originally planned. This document will
need revision as new capabilities, ports, and adapters are added; it
describes v0 only.

## Trust boundaries

Every value that crosses into a capability invocation carries a
`Provenance`: a `Trust` level and a `Classification`
(`domain/provenance.py`). Three trust levels exist:

- **`USER_DIRECT`** — typed by a human, directly, right now. Today
  this is the *only* trust level actually produced by any real call
  path: every CLI argument (`ping`'s absence of arguments, the music
  subcommand selection, `read`'s path argument) is wrapped via
  `Provenance.user()`.
- **`SYSTEM`** — produced by JARVIS's own internals. Defined and
  available (`Provenance.system()`), but nothing in the current
  codebase constructs it. No capability today produces a value on
  this trust level.
- **`UNTRUSTED_EXTERNAL`** — content originating outside the user's
  direct control (a web page, an email, a file whose provenance is
  unknown). Today this is produced in exactly one place: the
  *returned content* of `fs.read_file`, wrapped as
  `Provenance.external(source=<path>, classification=SENSITIVE)`
  (`kernel/files.py`). It has never yet been produced on an
  *invocation argument* — no current capability accepts an
  externally-sourced argument.

This last point matters for the tier ladder below: `evaluate()`'s
taint-escalation path (an `UNTRUSTED_EXTERNAL`-tainted argument bumps
the required tier by one step) is implemented and exercised by direct
domain-level tests, but has never yet fired from a real, end-to-end
capability invocation. The mechanism exists; nothing has triggered it
in practice yet.

## Classification

Four levels exist (`PUBLIC`/`PERSONAL`/`SENSITIVE`/`SECRET`). Today,
only two are ever actually assigned: `PUBLIC` (the implicit default via
`Provenance.user()`) and `SENSITIVE` (`fs.read_file`'s returned
content, per the reasoning in `kernel/files.py`'s own docstring — an
arbitrary local file's content is not assumed benign just because it's
local). No capability has ever produced a `PERSONAL` or `SECRET`
value. `SECRET`'s unconditional-deny-to-any-cloud-provider rule
(CLAUDE.md, ADR-0014) has never been exercised against a real value —
there is no cloud-routing code in this system yet at all.

## The tier ladder, and exactly what each tier checks

Four tiers, lowest restriction first: `ALLOW` → `CONFIRM` →
`MANUAL_ONLY` → `DENY`. `evaluate()` (`domain/policy.py`) is the sole
place this is decided:

- **`ALLOW`** — granted unconditionally. `context` is never read at
  all. Today: `ping` (`READ_LOCAL`) and `fs.read_file`
  (`EGRESS_LOCAL`).
- **`CONFIRM`** — granted if `physical_confirmation_available` **or**
  `remote_confirmation_available` (either channel suffices — an OR,
  not an AND). Today: `music.play`/`music.pause`/`music.next`/
  `music.previous` (`WRITE_LOCAL`).
- **`MANUAL_ONLY`** — granted only if `physical_confirmation_available`.
  `remote_confirmation_available` is deliberately never read for this
  tier, even if `True` — quoting `policy.py`'s own comment: *"per the
  M0 threat model, voice/remote confirmation is a convenience filter,
  never an authorization boundary (defeated by replay/cloning)."*
  **No capability registered today uses this tier.**
- **`DENY`** — an absolute ceiling; `granted` is always `False`,
  regardless of any confirmation. **No capability registered today
  uses this tier**, and taint-escalation has never pushed a real
  invocation here either (see above).

### The most important honesty point in this document

`ManualConfirmationAdapter` (`adapters/confirmation.py`) — the only
implementation of `ConfirmationPort` that exists — reports fixed,
**constructor-supplied** booleans. In the real CLI, those booleans come
directly from `--physical-confirmation-available` /
`--remote-confirmation-available` flags: plain, self-reported claims
typed by whoever is running the command, with **no presence sensor,
biometric check, hardware token, or any other real-world signal behind
them.**

Concretely: today, `CONFIRM` and `MANUAL_ONLY` provide *identical*
real-world protection, because both are satisfied by the same
unverified CLI flag — and no capability currently uses `MANUAL_ONLY`
anyway, so this hasn't yet been exercised even nominally. The tier
ladder is real, tested, and correctly enforced *given its inputs*; the
inputs themselves are not yet backed by anything a threat model can
credit as an authorization boundary. **A physical-presence-detection
adapter does not exist.** Anyone who can run the `jarvis` command with
normal user-level shell access can claim physical presence and have it
accepted at face value.

## `fs.read_file`'s path scope: a structural boundary, not a tier

`fs.read_file` is `ALLOW`-tier (see above) precisely because its real
protection is not the tier ladder at all: `kernel/files.py` resolves
every requested path (`expanduser().resolve()`, which follows symlinks
to their real target) and rejects it outright — before any
`CapabilityInvocation` is even constructed — if it falls outside
`allowed_root` (the real user's home directory, in production use).
No confirmation flag can override this. It is a single coarse
boundary (all of `$HOME`, not a denylist of sensitive subpaths like
`.ssh/`), deliberately: this project has consistently favored
allowlists over denylists (ADR-0007's spirit), and a denylist of
"known-sensitive" paths creates a false sense of completeness that a
coarse allowlist boundary does not.

## Known, explicitly-accepted gaps

Three gaps remain open and are stated here plainly, in decreasing
order of how directly they contradict something this project already
decided to do differently. They keep their original numbers (2–4)
rather than being renumbered after Gap 1 was closed, so that existing
references to a specific gap (`docs/plugin-guide/README.md`,
`docs/protocol/README.md`) stay accurate without needing to change in
lockstep with this document.

### Gap 1 (resolved, work package 18): the audit log used to store raw argument values, not digests

ADR-0027 decided: *"The audit log records only digests (hashes) of
capability invocation arguments, never the argument values
themselves."* CLAUDE.md repeats this as a hard privacy rule. From work
package 12 through work package 17, the actual implementation did not
do this: `adapters/audit_storage.py`'s `_encode_arguments()` wrote the
full, raw argument value directly into the persisted JSON audit file.
This was caught by this threat model (Milestone 0 closeout, work
package 17), not by any test, because no test at the time asserted the
*absence* of raw values from the persisted file — only that
round-tripping was correct.

Work package 18 fixed this at its root: `domain/audit.py`'s
`_canonicalize` — the same function that computes every
`AuditRecord.record_hash` — now treats a `Tainted` value as a special
case, reducing it to a sha256 digest of its `.value`
(`domain.audit.digest_value`) rather than walking into the value
itself. `adapters/audit_storage.py` persists that digest
(`"value_digest"`) instead of the raw value. Because the hash-chain's
own tamper detection is computed from the same canonical form the
persisted file is derived from, this is not merely "the file looks
safer" — a record whose persisted digest doesn't match what its
`record_hash` commits to is provably tampered, the same guarantee
every other field already had.

Two consequences, stated plainly:

- **This is a permanent, one-way change with no migration path.** A
  chain file written before work package 18 cannot be loaded by this
  or any later version: the raw argument values needed to recompute a
  valid digest under the new formula were never persisted, by design.
  Attempting to load one raises `KeyError` (missing `"value_digest"`),
  surfaced by the CLI as a clean `Error: ...` and exit `1`, not a raw
  traceback or a silent misparse.
- **Scope is argument *values* only.** `Provenance` (trust,
  classification, sources) attached to an argument is unaffected and
  still persists in full — ADR-0027 scopes the digest-only requirement
  to argument values, not their provenance metadata, and knowing an
  argument's trust/classification without its content is not the kind
  of exposure ADR-0027 was written to prevent.

The regression this gap was missing now exists:
`tests/unit/test_audit_storage_adapter.py::test_saved_file_never_contains_the_raw_argument_value`
asserts a marker string placed in an argument value never appears
anywhere in the persisted file's bytes, not just that round-tripping
happens to produce the right shape.

### Gap 2: no header/payload split, so nothing in the audit chain can ever be redacted

ADR-0028 decided audit entries would split into a header (hashed,
never redacted) and a payload (redactable independently, without
breaking the chain) — specifically to support cases like a
right-to-erasure request. The actual `AuditRecord`
(`domain/audit.py`) is flat: `sequence`, `decision`, `previous_hash`,
`record_hash`, with no header/payload distinction anywhere. Unlike Gap
1, this was never attempted, not contradicted — closer in kind to the
next gap. But the practical consequence is real: **there is currently
no way to redact anything from the audit chain without breaking
tamper-evidence for every record after it.**

### Gap 3: the hash chain detects tampering, but is not signed

Stated in `domain/audit.py`'s own module docstring since work package
5, quoted here rather than restated: *"this chain detects any
tampering with a single record, or any partial reordering that isn't
accompanied by recomputing every subsequent hash. It does NOT protect
against an attacker with write access to storage who is willing to
relabel sequence numbers and recompute hashes forward through the rest
of the chain — that requires a signing key, HMAC, or external
anchoring, none of which this module provides."*

### Gap 4: an out-of-scope file-read attempt leaves no audit record at all

Stated in `kernel/files.py`'s `PathOutsideAllowedScopeError` docstring
since work package 15, quoted here rather than restated: *"because
this rejection happens before any `CapabilityInvocation` is
constructed, out-of-scope access attempts are NOT recorded in the
audit chain — unlike a granted or normally-denied decision, there is
no forensic record of who tried to read what outside the allowed
root."* Closing this requires either extending `evaluate()`'s
escalation formula (a frozen work-package-4 change) or a separate,
out-of-band security-event log — neither exists yet.

## What is explicitly not yet defended against

Stated plainly, not implied:

- **No real physical-presence detection for direct CLI invocation.**
  See "the most important honesty point" above — still true exactly as
  written for `jarvis ping`/`play`/`pause`/`next`/`previous`/`read`'s
  `--physical-confirmation-available` flag, which remains a plain,
  self-reported claim. As of Milestone 1, this is **no longer true for
  voice-triggered invocation** (`jarvis listen`) — see "Milestone 1
  additions" below for the real, physical-keypress mechanism that path
  now goes through instead. The gap this bullet originally described
  has narrowed, not closed: it is now specific to the CLI's own flags,
  not the whole system.
- **No plugin sandboxing.** `jarvis.plugin_api` has no real content
  yet. Nothing untrusted loads or executes in this system today —
  every capability is hardcoded in this same source tree
  (`kernel/capabilities.py`). The moment third-party plugin loading
  exists, this threat model will need a real section on it; today
  there is nothing to defend because there is no plugin loader.
- **No signed or externally-anchored audit chain** (Gap 3, above).
- **No encryption at rest for the audit log.** It is a plain JSON
  file, readable by anyone with filesystem access to it.
- **No atomic writes for the audit file.** `adapters/audit_storage.py`
  states this in its own docstring: a crash mid-`save()` can leave a
  partially-written file. Not handled.
- **No verification that a `ConfirmationPort` adapter is telling the
  truth.** Even a future, more sophisticated confirmation adapter is
  only as trustworthy as its own implementation; nothing in the
  policy engine cross-checks a confirmation signal against anything
  else.

## Summary

The policy engine, capability registry, and audit chain's internal
consistency (per-record tamper detection, hash linkage, and now,
digest-only argument logging) are real, tested, and enforced exactly
as designed. What is *not* yet real: any actual authorization signal
behind `CONFIRM`/`MANUAL_ONLY` beyond a self-reported CLI flag, any
capability dangerous enough to need `MANUAL_ONLY`, redaction (Gap 2),
chain signing (Gap 3), and plugin sandboxing. Milestone 0 built
the enforcement machinery honestly; it has not yet been asked to
enforce anything under real adversarial pressure, because nothing in
the current capability set is dangerous enough to test it, and the
one thing that would matter most under pressure — a real presence
signal — does not exist yet.

## Milestone 1 additions

Appended per `docs/architecture/m1-voice-architecture.md` section 5,
with real content reflecting what was actually built (work packages
19–26), not the placeholder text that section originally sketched.
Everything above this heading describes Milestone 0 exactly as it was
at the time; nothing above has been rewritten to match Milestone 1 —
new facts are added here, not blended backward into the M0 snapshot.

### Always-on listening is a new, permanent privacy surface

Wake-word detection (`OpenWakeWordAdapter`, WP-20) runs continuously
on-device for as long as `jarvis listen` is running. It must never
touch the network — openWakeWord's tflite inference path is fully
local — and the ring buffer feeding it (`_AudioRingBuffer`) must never
be written to disk under any circumstance. This extends ADR-0018 ("no
audio persisted to disk") from "no audio persisted" to "no audio
persisted, including transiently during wake-word evaluation itself" —
checked directly: `_AudioRingBuffer` is a plain in-memory `deque`, with
no file I/O anywhere in its implementation, and this is exercised by
`tests/unit/test_wake_word_adapter.py`'s ring-buffer tests.

**A legal note, carried forward from the original M0 review, that
still applies and has not been re-derived:** recording someone else's
non-public spoken word without consent is treated seriously under
German law (§201 StGB). A household microphone that can capture
flatmates or visitors is exactly the situation that contemplates. The
ring-buffer-only, never-persisted design is a direct mitigation, not a
complete one — it limits what could ever be exfiltrated or recovered
after the fact, but does not by itself address consent for people
other than the primary user who happen to be audible to an always-on
microphone. This remains open, stated plainly rather than assumed
solved.

### Finding 2 is closed for voice-triggered invocation, not for the CLI's own flags

See ADR-0035 for the full design and the two real implementation bugs
manual verification caught. The claim, stated precisely: **as of
`PhysicalConfirmationPort`/`Gtk4PhysicalConfirmationAdapter` (WP-24)
and `kernel.voice_loop.run_voice_loop` (WP-25), any command resolved
from voice input requires a physical keypress or click — on a
GTK4 dialog whose Approve/Deny handlers verify a genuine `Gdk.Event`
backed by a real input device — that voice alone cannot produce.**
`tests/unit/test_confirmation_dialog.py` proves the underlying check
rejects a fabricated or missing event, closing the specific gap the
M1 doc named: *"this needs its own test proving a simulated or
injected keypress event is rejected the same way M0's original
synthetic-input interlock was designed to prevent"* — no such interlock
actually existed in M0 (checked directly: no synthetic-input handling
of any kind appears anywhere in the M0 source), so this test is the
first one, not a continuation of a prior mechanism.

This closure is intentionally narrow, matching what was actually
built rather than overclaiming: it applies to the `jarvis listen` path
specifically. `jarvis ping`/`play`/`pause`/`next`/`previous`/`read`'s
`--physical-confirmation-available` flag is completely unchanged —
still a self-reported CLI boolean, per "the most important honesty
point" above. `ManualConfirmationAdapter` was not modified, deprecated,
or replaced; it remains in normal use for every non-voice invocation,
per the M1 architecture doc's own recommendation (section 11, open
question 3) to run the two paths alongside each other rather than
unify them.

Scope, stated as honestly here as in ADR-0035 itself: the physical-
keypress mechanism defends against software-only confirmation forgery
(an in-process call faking a click, a voice recording, a network-
triggered invocation with no human at the keyboard at all) — it is not
claimed to defend against an attacker who already has root or
`/dev/uinput` access on the machine, a strictly stronger capability
than any of those. `MANUAL_ONLY` is still not bulletproof against an
attacker with full control of the session; it is now, for the first
time, backed by something voice/recording/remote access genuinely
cannot satisfy on its own.

### Speaker verification remains audit/UX only, now mechanically enforced

See ADR-0034. `SpeakerIdPort`/`UnverifiedSpeakerIdAdapter` (WP-23)
exist and are wired into `kernel.voice_loop.run_voice_loop`, but their
output is logged only — never read again, never used to construct a
`PolicyContext`. `tests/meta/test_speaker_id_isolation.py` verifies
this holds structurally across the whole `src/jarvis` tree, not just
by convention.

### A real, previously-undisclosed drift found and corrected during Track 6's full-repo health pass (2026-09-02): the `v0.2.0` tag's own exact commit was tagged while CI was genuinely red, not green

Found by walking every tag's own real commit SHA (careful to dereference
`v0.2.0` as an *annotated* tag object to its underlying commit, not the
tag object's own SHA, which `git rev-parse v0.2.0` alone returns and
which never matches any GitHub Actions `head_sha`) against the real
GitHub Actions run history via the API, not assumed from the tag's own
existence. Every tag from `v0.1.0` through `v0.5.0` checked this way;
five of six show a real, matching `conclusion: success` run against
their exact tagged commit. `v0.2.0` (M1's tag, commit `08f13f2`, tagged
2026-08-15) does not: its one and only real CI run
(`actions/runs/31899647509`) shows `conclusion: failure`, both matrix
jobs (`py3.12`, `py3.13`) failing at the `Install dependencies` step —
before any gate ever ran. The next commit on `main` after the tag
(`6dabb43`, 2026-08-17) failed the identical way. This was not a new
discovery in the sense of "no one ever noticed" — commit `8c1c6cef`
(2026-08-18, `ci: install system deps for pygobject's build, fixing CI
since WP-24 (8ffad01)`) already root-caused and fixed the underlying
break: `pygobject` (introduced by WP-24, `8ffad01`) ships no prebuilt
wheel, and the CI workflow had no system-package install step for its
build dependencies until that commit. What *was* newly found here is
narrower and more specific: this project's own docs never disclosed
that the fix landed **three days after `v0.2.0` was already tagged**,
meaning the exact commit the tag points to never itself had a passing
CI run, and `CLAUDE.md`'s/`docs/ROADMAP.md`'s own "all 7 gates pass, CI
green" language for M1 was true of `main`'s state days later, not of
the tagged commit specifically — a real, if narrow, overstatement,
corrected in both files by this same pass rather than left standing.
No code change follows from this: `main`'s tip has been green on every
push since `8c1c6cef`, `v0.2.0`'s own tree was never functionally
broken (the failure was 100% a CI-configuration gap, not a test/gate
failure), and retagging or moving an already-published tag is exactly
the kind of destructive, hard-to-reverse action this project's own
git discipline avoids without the user's explicit direction — the tag
stays where it is, the record is corrected instead.

## Milestone 2 additions

Appended per the same convention `docs/architecture/m1-voice-architecture.md`
section 5 established for Milestone 1: real content reflecting what
was actually built (work packages 31–40), not a rewrite of the M0/M1
snapshot above. New facts only, added here rather than blended
backward.

### Cloud egress to a reasoning provider is real for the first time

Milestone 0's `_EFFECT_TIER_FLOOR` already defined `EGRESS_SENSITIVE`/
`EGRESS_SECRET`, but "the current capability set" the M0 Summary above
names had nothing that declared either effect — checked directly at
the time (`kernel/capabilities.py`'s `build_default_registry()`
registered only `READ_LOCAL`/`WRITE_LOCAL`/`EGRESS_LOCAL`). `ModelRouter`
(WP-36) is the first real code that constructs a `CapabilityInvocation`
declaring `EGRESS_SENSITIVE` or `EGRESS_SECRET`, so ADR-0014/ADR-0015/
ADR-0038's Classification-based gating is exercised by real code,
through the real `AuthorizationOrchestrator`/`AuditChain` choke point
(ADR-0039), for the first time in this project's history — not merely
tested at the domain level in isolation, as it was after WP-30.

Recovered acceptance criterion #6 ("A SENSITIVE-classified task never
reaches a cloud provider at any rung") contradicted real, Accepted
ADR-0015 ("SENSITIVE data may be sent... behind an explicit CONFIRM").
Flagged during WP-36, resolved in ADR-0015's favor, not silently: a
SENSITIVE task can reach a cloud provider, but never without a real
confirmation channel present, and never by default —
`tests/property/test_router.py` asserts both halves of that claim
directly, not just the "never reaches" half the recovered wording
implied.

### A real, explicitly-accepted gap: candidate execution is not sandboxed

`WorkspacePort`/`LocalWorkspaceAdapter` (ADR-0043, WP-33) apply a
Candidate's content as a patch to real files on disk via `git apply`;
`RuntimeCheckValidator`, `PytestValidator`, and `UserScriptValidator`
then run real, caller-supplied commands against that patched workspace
via a plain subprocess, with the same privileges as the JARVIS process
itself — no container, no restricted user, no filesystem or network
limits, no resource caps. `FamilyAReasoningAdapter`/`FamilyBReasoningAdapter`
tag their output `Trust.UNTRUSTED_EXTERNAL` (WP-32) specifically
because a cloud provider's content is adversarially-manipulable, and a
validator that executes an `UNTRUSTED_EXTERNAL`-tainted Candidate's
content is, structurally, running attacker-influenceable code with
full process privileges the moment that candidate reaches a
`RuntimeCheckValidator`/`PytestValidator`/`UserScriptValidator` stage.

This is real, not hypothetical, and not fixed in M2: nothing in
`adapters/validation/` restricts what a patched workspace's code can
do once a validator's command is invoked. It is the same class of gap
deliverable #9 ("test-file protection in a coding agent's resource
scope") was meant to narrow, but broader — deliverable #9 itself
remains explicitly deferred to M5 (confirmed: no work package from
WP-32 through WP-40 restricts which files a Candidate's patch or a
validator's command may touch), and no sandboxing of any kind was
ever in this milestone's scope. Stated here rather than left implicit,
matching Gap 2–4's own precedent above: known, real, unresolved.

### Cassette files are plaintext, unencrypted regression fixtures

`CassetteRecorder`/`CassettePlayer` (ADR unrecorded as its own number —
see WP-38) write a real Candidate's full content, author, and
provenance to a plain JSON file under `tests/cassettes/`, with no
encryption, redaction, or access control. Recording a cassette from a
real task whose content happens to be `SENSITIVE`- or `SECRET`-classified
would permanently place that content in plaintext as a checked-in test
fixture. This is an operational discipline this milestone states but
does not mechanically enforce — no code in `CassetteRecorder` inspects
or gates on the wrapped provider's real output provenance before
writing it to disk. Nothing in this repository's real, checked-in
`tests/cassettes/` files carries any classification above `PUBLIC`
today; keeping it that way is a human discipline, not a checked
invariant.

### A referenced acceptance criterion was never actually written down

The work-package plan this milestone followed named "acceptance
criteria #9, #10, and #11" as added during the WP-28/29 planning pass.
Checked directly against every real ADR in `docs/adr/`: only #9
(ADR-0038, SECRET never egresses) and #11 (ADR-0039, no vendor name
leaks past `adapters/reasoning/`) are actually recorded anywhere in
this repository. Criterion #10 is referenced by number only, in
work-package orchestration text outside this repository's own history
— not in any ADR, not in `m2-reasoning-layer.md`, not anywhere
`git log`/`git grep` can find it. Stated here rather than silently
dropped or invented to fill the gap: what #10 was meant to assert is
genuinely unknown from anything checkable in this repository.

### Live verification of the reasoning adapters — tracked item, mirroring M1's #19

M2's closeout above flagged all three `ReasoningPort` adapters as
code-complete but never live-verified against a real provider. A
consolidation pass (post-`v0.3.0`) closed part of this gap for real,
one adapter at a time, and found the rest genuinely blocked rather
than silently left as before:

- **`local`**: fully live-verified. A real Ollama server (installed
  for this purpose, `llama3.2:1b` pulled) answered a real
  `LocalReasoningAdapter.generate()` call end to end — request shape,
  response-parsing shape, `Trust.SYSTEM` tagging, and the real
  `ModelRouter` → `AuthorizationOrchestrator` → `AuditChain` path (one
  real, hash-chained audit record produced) all confirmed correct
  against a live, real service. Confirmed absent, separately: no
  WP-28 PoC script was ever written or run, for any of the three
  adapters — checked directly against `poc/` and `git log`, not
  assumed.
- **`family_a`**: partially live-verified. `_ENDPOINT` was corrected
  from a deliberate `.example` placeholder (WP-32) to the real OpenAI
  Chat Completions host, and one real round-trip was attempted through
  the actual, unmodified `FamilyAReasoningAdapter`, with a real
  credential resolved via `SecretPort` (provisioned directly into the
  keyring by the user; never seen or handled as text by the assistant
  at any point). The request reached OpenAI and authenticated
  successfully — real evidence for the request shape (auth header,
  JSON body, `model`/`messages` fields) — but returned
  `insufficient_quota` (an account billing condition, not a shape
  error) before any response body existed to parse.
  `_extract_content`'s response-parsing shape remains genuinely
  unverified. The user explicitly declined to add API billing to
  resolve this, and separately asked whether the existing ChatGPT/Claude
  Pro web or desktop apps could be automated instead of the API —
  declined: that would be a new adapter (browser/app automation is a
  different integration entirely from an HTTP client), not a
  verification of this one, and automating a consumer subscription
  product outside its official API is very likely a Terms-of-Service
  violation for the user's account, not a design or scope choice this
  project should make on their behalf.
- **`family_b`**: not live-verified at all, by explicit, deliberate
  choice — no Anthropic credential was ever provisioned. Unit- and
  cassette-tested only.

None of this is treated as blocking: `v0.3.0` already shipped with
this gap honestly flagged, and this pass narrowed it rather than
being required to close it completely.

## Milestone 3 additions

Appended per the same convention `docs/threat-model/v0.md`'s own
Milestone 1/2 sections established: real content reflecting what was
actually built (WP-43 through WP-55), not a rewrite of the M0–M2
snapshot above. New facts only.

### Real sandboxing exists for the first time, but M2's own gap is not retrofitted

`SandboxPort`/`BwrapSandboxAdapter` (ADR-0044, WP-45) is this
project's first real, kernel-enforced containment mechanism —
genuinely verified live (not merely documented) during WP-45's own
test suite: a real `bwrap --unshare-all` sandbox denies outbound
network and denies filesystem access outside explicitly bound paths,
and an explicitly bound path is genuinely writable. This is exactly
the mechanism the "candidate execution is not sandboxed" gap (Milestone
2 additions, above) named as missing.

**Explicitly not retrofitted onto M2's validators**, decided before
this milestone's implementation began (`m3-desktop-control.md`'s
"Relationship to M2" section, ADR-0044's own "Consequences"):
`RuntimeCheckValidator`/`PytestValidator`/`UserScriptValidator` still
run real, caller-supplied commands with full JARVIS-process privileges
against `Trust.UNTRUSTED_EXTERNAL`-tagged cloud-provider content — the
exact same real, unresolved gap Milestone 2's own closeout already
named. `SandboxPort` is real, general-purpose infrastructure a future
work package *could* apply there; this milestone deliberately did not,
to stay scoped to desktop control rather than quietly expanding into
"M2 hardening." Stated here plainly: this gap is now *closeable* with
existing infrastructure, but is not yet closed.

### Terminal: a real, narrow, structurally-bounded exception to "no shell"

`terminal.run` (ADR-0046, WP-52) is the first capability in this
project's history whose whole purpose is accepting free-text input and
having it interpreted as a shell command — a deliberate, explicitly
argued exception to ADR-0003/ADR-0007, not a quiet erosion of them.
Three real, tested mitigations bound it: unconditional
`Tier.MANUAL_ONLY` (no `CONFIRM`-tier fast path, ever), a structural
guarantee that keystroke injection can only ever reach a terminal
window `SandboxPort` itself just launched (verified by AST scan,
`tests/meta/test_terminal_sandboxed_launch_only.py`, including a
self-test that the scan genuinely fires on a deliberate violation),
and unconditional `Trust.UNTRUSTED_EXTERNAL` tagging of any captured
output. **Never exercised against a real, visible terminal window
during this pass** — see "What was never live-verified" below.

### A new class of untrusted content: real desktop-application window state

`DesktopWindowPort.read_visible_text` (WP-44) introduces a genuinely
new source of `Trust.UNTRUSTED_EXTERNAL` content beyond M2's
cloud-provider responses: whatever text a real, running application's
own accessibility tree happens to expose. ADR-0011 already named this
class of content in the abstract ("a web page, a terminal's own
command output, an accessibility tree populated by an application this
process does not control"); this milestone is the first to build a
real port that can actually produce it. Real, live-confirmed during
WP-43's spike: AT-SPI2 access is not just gated by the *target*
application's own policy but by the *caller's* — a real `GetItems` call
against Brave's accessible tree, issued from this session's own
snap-confined shell, was denied by the snap D-Bus proxy independent of
any AppArmor policy on Brave itself. Whether a real, eventually-packaged
JARVIS process is itself snap-confined when this code actually runs
was not something this pass could control or verify in advance —
flagged as a real, environment-dependent risk, not assumed away.

### A real C6 layering violation, caught by the gate itself and fixed, not silently avoided

During WP-51, importing `AtspiDesktopWindowAdapter` directly into
`kernel/desktop.py` (to give `authorize_and_send_text_to_chat_app` a
real default, matching every sibling function's own shape) broke C6
("no GLib in the core") for real: `lint-imports` reported the genuine
transitive chain `jarvis.kernel.desktop -> jarvis.adapters.desktop_window
-> gi`. Fixed by making `desktop_window` (and, from WP-52 onward,
`sandbox`) required parameters with no default — mirroring
`kernel/voice_loop.py`'s own, already-established precedent for
`Gtk4PhysicalConfirmationAdapter` exactly. Recorded here because it is
a real, load-bearing example of this project's own gates catching a
genuine mistake mid-implementation, not a hypothetical the design docs
merely warned about.

### What was never live-verified — mirroring M1's #19 and M2's own closeout precedent

Every desktop-control capability this milestone built is
code-complete, gate-passing, and unit-tested against injected fakes —
but real, physical-world verification was deliberately withheld for
an entire category of action, per this run's own unattended-operation
rules (never touch the user's real, live desktop or real external
accounts while unsupervised):

- **No real window was ever found, focused, or typed into.**
  `AtspiDesktopWindowAdapter`'s live AT-SPI2 calls
  (`_atspi_find_app`/`_atspi_focus`/`_atspi_type_text`/`_atspi_read_text`)
  are real code, exercised only through injected fakes in every
  automated test — the same "real but untested-by-design" pattern
  `adapters/media_player.py`'s `_send_method_call_over_dbus` already
  established. This means **Brave, VS Code, the Claude app, the
  ChatGPT app, and Terminal's window-interaction step are all
  unverified against a real running application**, for all eight apps
  at once, not narrowed the way M2's per-adapter live-verification
  pass narrowed its own equivalent gap.
- **The Wayland `RemoteDesktop`/libei synthetic-input path named in the
  original M3 objective was never used at all.** WP-43 found the
  `libei`/`EIS` GI binding absent on the real development machine and
  chose AT-SPI2's `EditableText`/`Component` interfaces instead,
  partly because calling `RemoteDesktop.CreateSession` for real would
  have popped a real system permission dialog on the user's live
  screen during an unattended run. Whether AT-SPI2 alone proves
  sufficient for every real target application (especially Electron-
  based ones, which have historically inconsistent AT-SPI2 support)
  is genuinely unconfirmed.
- **`gnome-terminal`'s own AT-SPI2 `Text` interface support is
  unconfirmed** — chosen over Brave/VS Code specifically because it is
  a native, non-snap binary (avoiding the D-Bus-proxy denial class
  above), but whether it actually exposes readable terminal content via
  AT-SPI2 was never checked against a real, running instance.
- **The ChatGPT desktop app's existence on any real machine is
  unconfirmed.** WP-43's spike found a real, installed `claude-desktop`
  binary but no ChatGPT desktop app installed by any name, snap, or
  `.desktop` file on the development machine; `"chatgpt"` is used as
  `DesktopWindowPort`'s best-effort app-id match, not a verified one.
- **No real Docker daemon call was ever made**, including the
  read-only `docker.list_containers` — a deliberate, broader
  restriction than this run's own hard-stop rule strictly required
  (which named only `run`/`build` explicitly), applied for consistency
  and because this run had no way to confirm any given Docker
  environment was genuinely disposable. Live verification is deferred
  to when the user is present to supervise it directly, the same
  deferral shape M2's `family_a`/`family_b` live-verification gap used.
- **`git.push`/`git.force_push` were verified for real, but only
  against disposable local repositories** (`tests/unit/adapters/test_git.py`
  creates a fresh scratch working repo and a fresh local bare "remote"
  per test) — never against any real, network-reachable remote. A real
  fast-forward rejection and a real force-overwrite were both observed
  against real git subprocess behavior, which is more verification than
  Docker's capabilities received, but still zero verification against
  a real hosted remote (GitHub or otherwise).
- ~~`BwrapSandboxAdapter`'s CI behavior on a real GitHub-hosted runner is
  unconfirmed~~ **resolved, post-WP-55**: it was not just unconfirmed but
  genuinely broken on `ubuntu-latest` — every WP-45+ CI run failed for
  real, with `bwrap: loopback: Failed RTM_NEWADDR: Operation not
  permitted`. Root cause, confirmed against the real runner: `bwrap`
  successfully creates its unprivileged user+network namespace (Ubuntu's
  `bubblewrap` AppArmor profile does permit that), but the netlink call
  it then makes to bring up the sandbox's own loopback interface is
  denied — `ubuntu-latest`'s `kernel.apparmor_restrict_unprivileged_userns=1`
  default blocks `CAP_NET_ADMIN` inside the new namespace regardless of
  the profile permitting the namespace's own creation, a known, widely-
  reported issue affecting other bwrap-based sandboxes on GitHub-hosted
  runners generally, not specific to this adapter. Fixed in
  `.github/workflows/ci.yml` with one added step
  (`sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0`,
  scoped to the ephemeral runner VM's own lifetime, touching nothing the
  sandbox itself contains once created) and verified live: every
  `BwrapSandboxAdapter` test — real network denial, real filesystem
  denial, real bind-path writes, real exit-code capture, real
  non-blocking `launch()` — passes for real on a real `ubuntu-latest`
  runner, both Python matrix legs, not skipped or weakened.
- **No numeric resource limits** (CPU/memory/pid caps) exist on any
  `SandboxPort`-contained process beyond filesystem/network isolation
  — a sandboxed command can still exhaust host CPU/memory/disk. Named
  as a real, unaddressed gap in ADR-0044 itself, restated here.

None of this is treated as blocking: this milestone's own acceptance
criteria (`m3-desktop-control.md`) already anticipated most of these
as open questions rather than promises, and this closeout records what
was actually found rather than rounding any of it up to "verified."

## Live desktop-control verification (post-WP-55, real apps, real machine)

A follow-on pass against the "what was never live-verified" list
above, mirroring M2's own `local`/`family_a` live-verification
consolidation: began supervised (user physically present, watching),
switched to unattended partway through when the user stepped away.
Real findings, not re-derived from the earlier, code-complete-but-
unverified state:

### ChatGPT desktop app: confirmed not installable as "the" app

Re-checked exhaustively (PATH, all `.desktop` locations, `snap list`,
`flatpak list`, `dpkg`) -- still absent, matching WP-43. New, more
informative fact found this pass: **OpenAI does not publish an
official Linux ChatGPT desktop client at all** -- every snap-store
result (`chatgpt-desktop`, `chatgpt-linux`, `chatgpt-desktop-client`,
etc.) is an unofficial third-party wrapper by an individual developer,
unlike `claude-desktop`, which is Anthropic's own official Linux
package. Decided, with the user, not to install one: it wouldn't
actually verify "the ChatGPT desktop app" as ADR-0045 means it, only
the general AT-SPI2/Electron mechanism, at the cost of running
unaudited third-party code. Remains deferred, now for a clearer reason.

### A real, previously-undiscovered bug: SandboxPort.launch() could not display anything

`BwrapSandboxAdapter.launch()` never bound anything under `/run`, so
no GUI application launched through it could connect to the display
server at all -- confirmed live: a real, sandboxed `gnome-terminal`
failed immediately with `Cannot open display:`. This was a real,
shipped bug in WP-52's own Terminal capability, not merely an
unverified claim: **Terminal could not have displayed a real window
even once the WP-45+ CI fix landed.**

Root-caused live, including ruling out a false lead: an early test
appeared to show `--unshare-net` itself breaking Wayland access, which
would have been a genuine, hard architectural tension (the network
isolation Terminal's sandbox most needs colliding with its ability to
display anything at all, forcing a choice between ADR-0044's isolation
guarantee and ADR-0046's terminal-must-work requirement). Retested with
a genuinely clean environment and this was **not confirmed** -- the
original test's own shell (this verification session's own snap-
confined VS Code terminal) was polluting `GTK_PATH`/`GIO_MODULE_DIR`/
`LOCPATH` with paths that don't exist inside the sandbox, producing a
misleading symptom. With a clean environment, full `--unshare-all`
(network included) launches a real GUI application successfully once
the right sockets are bound. Real fix landed:
`_display_bind_paths()` (`adapters/sandbox.py`) binds the real Wayland
socket (`$XDG_RUNTIME_DIR/$WAYLAND_DISPLAY`) and D-Bus session socket
(`$XDG_RUNTIME_DIR/bus`) when `SandboxPort.launch(..., allow_display=
True)` is set; `run_in_sandboxed_terminal` now passes it. Verified
live: a real sandboxed `gnome-terminal` genuinely displays and runs a
real inner command (`tests/unit/adapters/test_sandbox.py::
test_launch_with_allow_display_can_really_display_a_real_gui_app`,
skipped on headless CI with a named reason, not weakened). X11 (as
opposed to Wayland) was not covered -- no real X11 session was
available to test against on this machine.

### A real, structural gap: Terminal's own type_text mechanism cannot type into a real terminal

With display now working, checked the VTE terminal widget's real
AT-SPI2 interfaces directly: it exposes `Text` (read works -- output
capture is genuinely possible) but **not `EditableText`**.
`AtspiDesktopWindowAdapter.type_text()`'s only mechanism is finding a
focused `EditableText` node and calling `insert_text` on it -- which
means **`terminal.run`'s real flow, as shipped, cannot type a command
into a real terminal at all**, the one app this capability exists for.
This is a genuine, structural limitation of the AT-SPI2-only mechanism
WP-43 chose (over the original "portal + libei" objective, set aside
because libei's GI binding was absent) -- terminals are not
conventional text-entry widgets and were never going to expose
`EditableText`. A real fix needs synthetic keyboard input (the
RemoteDesktop portal, or libei once packaged), not another AT-SPI2
technique. Not fixed in this pass -- flagged as a real, open,
architecturally-significant gap, not routed around. Investigated in
full afterward, live-checked findings and a real recommendation (the
`RemoteDesktop` portal's direct D-Bus methods, not raw `libei` FFI
bindings) written up in
[`m3-terminal-synthetic-input-proposal.md`](../architecture/m3-terminal-synthetic-input-proposal.md)
-- a proposal only, no code, no ADR written yet.

**Update (WP-56):** the proposal above was resolved into
[ADR-0047](../adr/0047-syntheticinputport-remotedesktop-portal-for-terminal-typing.md),
**Accepted** by the user with a load-bearing real-time indicator
condition, and then implemented -- `SyntheticInputPort`/
`PortalSyntheticInputAdapter`, the per-character fail-closed
focus-verification loop, the dedicated GSettings terminal profile
(visual half of the indicator, live-created and verified idempotent on
this real machine), and `SecretPort.set_secret` (the `restore_token`
write path, live-verified round-trip against this machine's real
gnome-keyring) are all real, gate-clean, and unit-tested against fakes.
**Genuinely not done, stated plainly rather than rounded up:** the
actual `RemoteDesktop.CreateSession`/`SelectDevices`/`Start` D-Bus
calls were never exercised against the real portal this pass --
deliberately, since the first real call pops a real, interactive OS
permission dialog needing the user physically present, explicitly out
of scope for this unattended pass (see `adapters/synthetic_input.py`'s
own module docstring for the full reasoning). This is the one real-D-Bus
adapter in this repo whose wire mechanics were not even manually
verified, unlike every other one -- the live-verification session is
where this closes: a real portal dialog, a real amber/red-bordered
terminal, a real spoken announcement, and a real typed command, all
watched by a physically-present human per ADR-0047's own design.

### Brave: still genuinely denied, reconfirmed live

`find_or_launch("brave", ...)` fails for real today, against a Brave
instance that was already running: the same snap D-Bus proxy AppArmor
denial WP-43 found (`An AppArmor policy prevents this sender from
sending this message to this recipient`, `label="snap.brave.brave
(enforce)"`) is still present. This is a property of the calling
process's own confinement (this verification session's shell is itself
snap-confined, labeled `snap.code.code`), not something JARVIS's own
code can route around -- whether a real, eventually-packaged JARVIS
process is itself snap-confined when this code actually runs remains
the deciding factor, unverified either way.

### VS Code: works, with an honest partial result

`find_or_launch`/`focus()` succeed for real against a real, running VS
Code. `read_visible_text()` returns `None` -- not a failure, but a
real demonstration of the port's own documented scope limit ("only
checks the app itself, not descendants"): VS Code's actual editor
content lives deeper in its accessible tree, which this method does
not walk. A fuller check would need the same descendant-walk
`gnome-terminal`'s own verification used.

### Follow-up (2026-08-24): the VS Code and Brave findings above, re-investigated

A later, bounded pass re-opened exactly the two items above (no new
capability, no ADR) with two real, live-verified outcomes -- one a
genuine partial fix, one a full reversal of the earlier diagnosis.

**VS Code:** the fix above (`_atspi_read_text()` walks descendants,
bounded to 500 nodes, first-non-empty-wins) is implemented and is
correct on its own terms, but a live dump of VS Code's actual
accessible tree (`Atspi.get_desktop(0)` enumeration, full DFS) showed
the original diagnosis was wrong: the tree isn't deep, it's nearly
**empty** -- 2 reachable nodes total (`application`, `frame`), neither
exposing usable text. The real cause is that this machine's GNOME
accessibility bridge is off (`org.gnome.desktop.interface
toolkit-accessibility`, `org.gnome.desktop.a11y.applications
screen-reader-enabled`, and `org.a11y.Status.IsEnabled` all confirmed
`false` via live `gsettings`/`gdbus` queries) -- Chromium/Electron apps
(VS Code included) gate full AT-SPI2 tree publication behind this
toggle for performance, the same root cause already identified for the
Claude desktop app below. A per-process alternative
(`--force-renderer-accessibility`) was identified as a real candidate
but could not be verified live without fully quitting and relaunching
the user's actual, already-running VS Code window -- too disruptive to
do unattended, so it was not attempted. Enabling the bridge system-wide
would be exactly the kind of persistent, machine-wide change this pass
was told to avoid absent a narrow/reversible path. Net result: the
descendant-walk fix is real, committed, and a genuine improvement for
any app whose content sits below the root but *within* a published
tree -- it does not, by itself, close VS Code's gap, whose real fix is
a decision (accept the system-wide toggle, or scope
`--force-renderer-accessibility` into JARVIS's own VS Code launch path
and verify it against a fresh window) rather than a bounded code change.

**Brave:** re-diagnosed with the actual on-the-wire denial
(`journalctl`, not re-derived): `apparmor="DENIED" ... bus=
"accessibility" ... label="snap.brave.brave" ... peer_label=
"snap.code.code"`. The denying profile is **Brave's own** snap
confinement refusing to `receive` accessibility-bus messages from a
peer labeled `snap.code.code` -- not a restriction on the caller's
outgoing message, and not something `snap connections`/interface
wiring can fix (`snap interface accessibility` returns "no such
interface"; snapd has no such interface to connect on this system).
The decisive test: the same AT-SPI2 query, run via `systemd-run --user`
(a real, unconfined process -- confirmed via
`/proc/self/attr/current` reading `unconfined`, escaping this
session's own `snap.code.code` shell entirely) against the same,
already-running Brave instance, **succeeded** -- `get_child_count()`
returned `5`, zero AppArmor denials logged. This means the original
"still genuinely denied" finding above was correct only for queries
made *from this testing session's own confined shell*, not for
JARVIS's actual runtime: JARVIS is a plain, unpackaged Python process,
not itself snap-confined, and an unconfined peer is exactly what
Brave's profile does not deny. **No fix was needed and none was
applied** -- there is no real gap in JARVIS's own capability here, only
an artifact of testing through this particular tool's confined shell.
This is a genuine reversal of the earlier finding, not a new mitigation.

### Claude desktop app: launches for real, but never exposes an accessible tree

`find_or_launch("claude", ("claude-desktop",))` really launches the
app (confirmed via its real process tree) but its window never
appeared in `Atspi.get_desktop(0)`'s enumeration over a full 30-second
poll, before or after launch. Distinct from Brave's case: no AppArmor
denial, no error at all -- a genuine, silent absence, consistent with
Chromium/Electron's real, documented behavior of not activating its
AT-SPI2 accessibility bridge without a detected assistive-technology
client present. Real, practical consequence: **WP-51's
`desktop.claude_app_send_text` capability cannot be live-verified
against the real app in its default configuration** -- the same
`EditableText`-reachability question Terminal hit was never even
reached, because `find_or_launch` itself never succeeds.

### Docker: real, read-only call verified; DESTRUCTIVE correctly deferred

`authorize_and_list_docker_containers()`, called for real against this
machine's real, active Docker daemon, returned `granted=True`,
`Tier.ALLOW`, and real container names. `docker.run_container`/
`docker.build_image` were not attempted -- no human was present to
approve a DESTRUCTIVE-tier action in the moment, per this project's own
non-negotiable rule; still open, still deferred, matching M2's
`family_a`/`family_b` deferral pattern exactly.

### Git: real push/branch/commit/force-push, verified against a real, network-reachable remote

A disposable, private scratch GitHub repository
(`jarvis-m3-git-scratch-verify`) was created solely for this
verification -- `jarvis-ai-os`'s own history was never touched. Real
`authorize_and_push_git`/`authorize_and_create_git_branch`/
`authorize_and_commit_git`/`authorize_and_force_push_git` calls, through
the real `GitCliAdapter`, all succeeded against this real remote over a
real network connection -- the first time any Git capability was
verified beyond a local, disposable repo. A genuine non-fast-forward
rejection was not re-demonstrated against this remote specifically
(the verification script's own divergence step was a no-op, a real
mistake in the script, not the adapter); that exact behavior is
already covered for real by `tests/unit/adapters/test_git.py`'s local
bare-repo tests. The scratch repo could not be deleted afterward --
the available GitHub token lacks the `delete_repo` scope -- confirmed
private (no sensitive content beyond a test README) and left for the
user to delete manually: https://github.com/akashpolkar29/jarvis-m3-git-scratch-verify.

## Overnight live-reality audit (WP-56 continuation, 2026-08-25)

An unattended pass through every capability across M0-M3 that had
never been exercised against real, running system state -- same
standard as the finding that closed the VS Code `read_visible_text`
gap and reversed the Brave AppArmor finding: don't trust an earlier
"should work" claim, actually run it. Bounded to read-only, idempotent,
or app-launching actions -- no DESTRUCTIVE action, no real synthetic
keystroke, no real portal call (`SyntheticInputPort` stayed
untouched, per explicit instruction).

### M0's MPRIS media-player capability: confirmed real, for the first time

`MprisMediaPlayerAdapter` (M0) had never been exercised against a
real, running media player -- its own module docstring already
admitted this ("proven by manual verification instead," but no such
verification was ever actually logged anywhere). Spotify was found
genuinely running on this real machine (`org.mpris.MediaPlayer2.spotify`
on the session bus). The real adapter's `play()`/`pause()` were called
for real -- Spotify's own `PlaybackStatus` property genuinely
transitioned Paused -> Playing -> Paused, confirmed via a separate,
independent read before and after each call, and the player was left
in its original (paused) state. This is real, live confirmation of
both M0's own MPRIS story and M3's own Spotify deliverable
(`m3-desktop-control.md`'s "extend, not build" framing for Spotify) in
one test, since M3 never built a separate Spotify capability.

Same AppArmor confinement pattern already found for Brave applies here
too: this verification session's own snap-confined shell was denied by
Spotify's snap profile; an unconfined process (`systemd-run --user`,
the same technique that reversed the Brave finding) succeeded cleanly.
Consistent with the existing conclusion: this is an artifact of the
testing shell, not of JARVIS's own (unconfined) real runtime.

### Brave's `open_url` and VS Code's `open_file`: confirmed real, for the first time

Both adapters' own module docstrings previously stated their real
launch subprocess call was "never exercised for real during this
unattended pass, deliberately" -- launching a visible window was
treated as an uninvited real-desktop action in every prior pass. This
pass's own explicit scope authorized app-launching actions, so both
were exercised for real: `BraveCliAdapter.open_url("https://example.com")`
and `VsCodeCliAdapter.open_file(".../CLAUDE.md")` each completed
without exception, and a separate, independent AT-SPI2 check
afterward confirmed both apps' windows genuinely exist and are
discoverable. First real, live confirmation of either adapter's actual
subprocess-launch mechanics, not just its dispatch logic against fakes.

### A real, reproducible bug found and fixed: `focus()`/`is_focused()`/`is_visible_and_showing()` all checked the wrong AT-SPI2 node

The most significant finding of this pass. Exercising WP-56's own new
`is_focused()`/`is_visible_and_showing()` methods against a real
window for the first time (they shipped entirely untested against real
AT-SPI2 state) found: `focus()` genuinely failed, reproducibly, five
consecutive attempts, against a real, running `gnome-terminal` window
that was demonstrably visible and active on the real screen the whole
time. Root-caused, not guessed: a full AT-SPI2 tree dump found the
root `application` node (`find_or_launch`'s own handle target) has
**no** `Component` interface, **no** `Action` interface, and
`VISIBLE`/`SHOWING` both `False` -- always, regardless of the real
window's actual state -- while its `frame`-role child, one level down,
correctly reported a real, working `Component` interface and
`VISIBLE=True`/`SHOWING=True`/`ACTIVE=True`. A further, full tree dump
found the real focused widget (VTE's own `terminal`-role node,
`FOCUSED=True`) sitting seven levels deep, well past both the root and
its frame child.

This is the exact same category of bug `read_visible_text` already had
one confirmed instance of (the VS Code finding, post-M3) -- checking
only the top-level object was an unverified assumption baked into
`_atspi_focus` since WP-43 and copied into WP-56's new
`_atspi_is_focused`/`_atspi_is_visible_and_showing` without ever being
checked against a real window. **Fixed the same pass**: all three now
target a new `_find_frame_descendant()` helper's result (a bounded
walk for the first `frame`/`window`-role descendant) instead of the
root object directly; `is_focused()` additionally walks the full
subtree (focus can live arbitrarily deep, not just at the frame level)
matching `_find_focused_editable`'s own already-established pattern.
Re-verified live, same method, after the fix: `focus()` succeeded on
the first attempt, `is_visible_and_showing()` and `is_focused()` both
correctly returned `True` immediately afterward.

**Why this matters beyond a bug fix**: this bug sat directly inside
ADR-0047's own load-bearing safety mechanism -- had it shipped to the
live-verification session unfixed, `is_focused()` would have
structurally never returned `True` for a real `gnome-terminal` window,
meaning the very first per-character focus check in
`run_in_sandboxed_terminal`'s synthetic-typing loop would have aborted
immediately, before a single real character could ever be typed. This
would not have been unsafe (fail-closed correctly means no keystroke
fires when focus can't be verified) but would have made Terminal's new
capability completely non-functional the first time it was ever
exercised for real, for a reason having nothing to do with the actual
TOCTOU race ADR-0047 disclosed. Caught and fixed here instead, without
ever touching `SyntheticInputPort` or firing a real portal call.

No new automated test was added for this fix -- `_find_frame_descendant`
and the three corrected functions remain in this module's existing
"real, untested-by-design, proven by live verification" category
(same convention as every other real AT-SPI2 function here); this pass
*is* that live verification, the first one these three functions ever
got.

### M1's voice pipeline: real capture, VAD, STT, and TTS all confirmed working; tracker #19 narrowed, not closed

CLAUDE.md's tracker #19 states live end-to-end voice verification
remains open, with the bug "isolated to the capture code's own audio
path." Real, safe (no live "hey jarvis" utterance needed, since none
of what follows requires a human) diagnostics were run against this
machine's real microphone and GPU, using the actual shipped adapter
code, not hand-written substitutes:

- **Raw capture** (`OpenWakeWordAdapter`'s real device resolution +
  `sd.InputStream`, at the exact production sample rate/blocksize):
  opened a real input device, captured real int16 samples, correct
  shape/dtype, no overflow.
- **Full wake-word scoring pipeline** (`_default_frame_source`, real
  tflite model load + inference): ran for real on real captured audio,
  produced sane near-zero scores on quiet background noise (correctly
  not firing), no crash.
- **VAD** (`SileroVadAdapter.segment`, real Silero model): loaded and
  ran on a real 3s captured chunk, correctly reported zero speech
  segments (there was none).
- **STT** (`FasterWhisperAdapter.transcribe`, real faster-whisper model
  on this machine's real CUDA GPU): loaded and ran without error --
  the module's own documented past bug (ctranslate2/`LD_LIBRARY_PATH`)
  does not reproduce, the fix holds. Real, honestly-noted caveat: on
  near-silent input it returned a hallucinated transcript ("Thanks for
  watching!"), a well-documented Whisper failure mode on low-energy
  audio, not a JARVIS-specific bug -- and one the real pipeline's own
  VAD-gates-STT design (STT is only ever invoked on a VAD-confirmed
  speech segment) already structurally prevents from being reachable
  in the real `run_voice_loop` flow.
- **TTS** (`PiperTtsAdapter.speak`, real Piper model): synthesized
  real, strong-amplitude speech audio (nowhere near silence), no error.

**Genuinely not closed by this pass, stated plainly**: none of the
above required an actual "hey jarvis" utterance or a full
`run_voice_loop` integration run -- both need a physically-present
human, correctly out of scope for an unattended pass. Tracker #19's
specific failure mode was not reproduced, but was also not ruled out --
it may live in real wake-word *detection accuracy* (only testable by a
human actually saying the phrase) or in the `stream()`/`run_voice_loop`
integration layer, neither of which this pass could safely exercise.
What this pass does establish, for the first time with real evidence
rather than an unverified docstring claim: every individual pipeline
stage's own mechanics -- device access, model loading, inference,
real hardware (mic, GPU) -- work correctly in isolation. This
meaningfully narrows where tracker #19's real bug can be hiding; it
does not close the tracker.

### Also confirmed, matching the established pattern: real CreateItem write, real GSettings profile

Not new this pass (already documented under ADR-0047/WP-56 above) but
consistent with this section's own standard: `SecretPort.set_secret()`
and the real-time indicator's GSettings terminal profile were both
already live-verified for real against this machine during WP-56
itself, before this overnight pass began.

## Milestone 4 additions

Appended per the same convention `docs/threat-model/v0.md`'s own
Milestone 1/2/3 sections established: real content reflecting what was
actually built (WP-57 through WP-64), not a rewrite of the M0–M3
snapshot above. New facts only.

This entire milestone was built in one unattended, unsupervised
overnight pass, with the user physically away from the machine and
unreachable for questions -- the same "never touch the user's real,
live desktop or real external accounts while unsupervised" rule M3's
own closeout already invoked, extended here to cover an entire
milestone rather than one category of action within it. Every ADR
acceptance, real vector-store decision, and voice-grammar addition
below happened without the ability to ask a clarifying question
mid-implementation; each real judgment call is named as such, not
smoothed over.

### A real ADR-acceptance boundary was crossed, once, by necessity -- named plainly, not hidden

ADR-0048 through ADR-0053 were accepted directly by the user (via an
explicit two-question exchange before this pass began: "accept all six
as-is," "build everything, WP-57 through WP-64") -- a real, recorded
user decision, not assumed.

**ADR-0054 (ClockPort/IdPort) was different: it was authored and
marked Accepted unilaterally, mid-implementation, by this pass itself,
not by the user.** CLAUDE.md's own hard rule -- "no work package may
start until the user marks these Accepted" -- was not literally
followed for this one document. The reasoning at the time (see
ADR-0054 itself): `CLAUDE.md`'s own invariant text already presupposed
`ClockPort`/`IdPort` existed ("inject ClockPort / IdPort instead"),
nothing in `src/` had ever built them, WP-51's retention mechanics
could not proceed without a real, injectable time source without
either hand-rolling one from scratch or violating the project's own
banned-API gate, and ADR-0042 (`SecretPort`) is a real, already-user-
approved precedent for exactly this shape of "presupposed but never
built" gap. That reasoning is real and, on its own terms, sound -- but
it does not change the fact that a document governing this project's
own domain-layer invariants was self-approved during a pass whose own
governing rule says only the user does that. **Flagged here explicitly
for the user's own retroactive review**, not silently presented as
equivalent to the other six ADRs' real, direct acceptance. If the
user's own read of ADR-0054 differs from this pass's reasoning, it is
the one document in this milestone that may need to be reopened, not
merely noted.

### The benchmark-backed brute-force decision was made for real, not asserted

`m4-memory-retrieval.md`'s own exit gate required "brute-force-vs-ANN
decision made by benchmark, not preference." `poc/wp61_vector_store_benchmark.py`
was actually run on this real machine (RTX 5070 Laptop GPU, 8GB VRAM,
matching `m4-scoping-notes.md`'s own recorded constraints);
`docs/architecture/m4-benchmark-results.md` records the real numbers:
brute-force numpy cosine similarity beats `sqlite-vec` below roughly
1,000 records and loses by roughly 2x at 50,000, but even
brute-force's worst measured latency (27ms at 50k) is far below any
perceptible threshold, and 50k already exceeds this milestone's own
expected real scale (thousands to low tens of thousands, per scoping).
Brute-force was chosen; `sqlite-vec` was evaluated fairly and is not a
project dependency. Correctness on the real, hand-built eval set was
4/5 (later 6/7 with two added negative-control pairs) -- the one miss
is a genuine, honestly-reported near-tie (cosine similarity 0.703 vs.
0.690) on a deliberately included lexically-confusable query pair, not
hidden or tuned away by rewording the eval set after the fact.

### The embedding model choice was a real, deliberate risk trade-off, not a default

`fastembed`/`BAAI/bge-small-en-v1.5` (ONNX runtime, CPU-only, ~130MB,
downloads from Hugging Face on first real use) was chosen over a
`torch`-based sentence-transformer model specifically because this
pass ran unattended overnight: a multi-GB `torch`+CUDA install and
model download, with no one present to debug a driver-version
mismatch, was judged a real, avoidable risk to the whole night's
progress, not a hypothetical one. This project already depends on
`onnxruntime` (`faster-whisper`, wake-word inference), so this adds no
new ML-runtime dependency family. **Real, honestly-named trade-off**:
this is a smaller, lower-quality embedding model than a modern
sentence-transformer would provide, chosen for unattended-overnight
safety over embedding quality -- a real, tracked candidate for
revisiting once a human is present to supervise a heavier install, not
presented as the permanent, final word on embedding quality.

### The write-time SECRET denial and the retrieval-side SECRET filter were both proven live, but end-to-end write/retrieve was verified only once, manually, outside the automated suite

The full real pipeline -- `FastEmbedAdapter` (real model, real
download) feeding `SqliteMemoryAdapter` (real SQLite file, real
`ClockPort`/`IdPort`) -- was exercised once, live, in this session: three
real facts were written and a real query correctly recalled the right
one by real cosine similarity over real embeddings. This is real,
positive evidence the whole chain works end-to-end on this real
machine, on this one occasion. It is not repeated by CI and not
repeated automatically ever again -- matching this project's own
established precedent for cloud-provider reasoning adapters (cassette
replay, never a live network call in CI) and hardware-dependent
adapters (`test_sandbox.py`'s real-GUI test, skipped outside a real
Wayland session): the automated suite exercises every real line of
`adapters/embedding.py`/`adapters/memory.py` except the real model
load and real embedding computation themselves, via a deterministic
fake `EmbeddingPort` throughout.

### M4-gap-closure pass (2026-08-31): four named gaps closed

A separate, later pass -- not part of the original overnight M4
implementation above -- closed four of the gaps this section
originally named, before M4 was tagged. Each is marked "**Closed
2026-08-31**" in place below, with a pointer to the real commit that
closed it, rather than deleted outright: this project's own "don't
round up, record accurately" discipline applies to closing a gap the
same way it applies to opening one. New work in this pass:
`MemoryWritePort.sweep_expired()`/`forget()`, `authorize_and_pin()`/
`authorize_and_forget()` (new `memory.pin`/`memory.forget`
capabilities, kernel-level and tested, no new voice grammar per this
pass's own fixed scope), and real `jarvis.cli.main` subcommands
(`memory write`/`retrieve`/`forget`/`pin`).

**One real, named correction made during this pass, not a new
gap**: the "No CLI subcommand exists" bullet below originally claimed
the absence "mirror[ed] M3's own `docker.*`/`git.*` precedent" -- that
was a misreading, not a real precedent. `kernel/desktop.py`'s own
Docker/Git kernel functions were never wired into `jarvis.cli.main`
either, but nothing in this project ever decided that as a "capabilities
deliberately get no CLI" convention worth replicating; it was simply
never done. This pass's own CLI subcommands follow the only real
precedent `jarvis.cli.main` has ever had for what a capability's CLI
wrapper looks like -- `ping`/`play`/`pause`/`next`/`previous`/`read`
themselves. Docker/Git remain unwired, unchanged, out of this pass's
own scope; see `src/jarvis/cli/main.py`'s own module docstring for the
corrected version of this reasoning in place.

### Real, deliberate scope limits and gaps this milestone does not close

Named plainly, mirroring M1/M2/M3's own closeout discipline -- nothing
below is rounded up to "done":

- ~~No garbage-collection sweep exists.~~ **Closed 2026-08-31**
  (`feat: WP-65a`). `MemoryWritePort.sweep_expired()` now permanently
  deletes expired, unpinned rows from the real SQLite file, triggered
  on every granted `memory.write` call. Verified against the real
  underlying table via a second, raw connection, not just via
  `retrieve()`'s own filtering. `exclude_expired_records` (ADR-0051)
  still does its own, separate query-time-exclusion job unchanged --
  this closes the "nothing ever actually deletes" half of the original
  gap, not a redesign of the other half.
- ~~`MemoryWritePort.pin()` has no real caller anywhere in this
  codebase.~~ **Closed 2026-08-31** (`feat: WP-65b`).
  `authorize_and_pin()` is a new, real caller: a statically-registered
  `memory.pin` capability (`Effect.WRITE_LOCAL`, `Tier.CONFIRM`),
  authorized via the same `authorize_by_id()` path as
  `memory.retrieve`. Kernel-level and tested only, per this pass's own
  fixed scope -- no voice grammar or further CLI beyond
  `jarvis memory pin <id>` was added.
- ~~No "forget X" deletion capability exists.~~ **Closed 2026-08-31**
  (`feat: WP-65c`). `MemoryWritePort.forget()` permanently deletes a
  record by identifier -- real deletion, not marking-for-expiry, the
  same bar `sweep_expired()` holds itself to, and one that deletes a
  pinned record too (pinning protects against *automatic* TTL expiry,
  not an explicit, targeted forget). `authorize_and_forget()` is the
  new `memory.forget` capability (`Effect.DESTRUCTIVE |
  Effect.IRREVERSIBLE`, `Tier.MANUAL_ONLY`) -- the same effect
  combination and reasoning `git.force_push` already uses for an
  unrecoverable action.
- ~~No CLI subcommand exists for any memory capability.~~ **Closed
  2026-08-31** (`feat: WP-65d`). `jarvis memory write/retrieve/forget/pin`
  are real subcommands in `jarvis.cli.main`, each a thin wrapper
  around the matching `authorize_and_*` kernel function -- see this
  section's own "real, named correction" note above for why the
  original "mirrors `docker.*`/`git.*`'s no-CLI precedent" reasoning
  for this gap's *existence* was itself based on a misreading.
- ~~ADR-0050's provenance carry-forward discipline has no real
  consumer to exercise it yet.~~ **Closed 2026-09-02**
  (`feat: overnight Track 2`). `application/memory/carry_forward.py::authorize_reasoning_call_with_recalled_context`
  is a real consumer: it recalls a `MemoryRecord`, merges its own real
  provenance with the caller's own additional task provenance (via
  `Tainted.combine()`/`Provenance.merge()` -- "the maximum trust, the
  maximum classification" of the two, not the record's own alone),
  and feeds the result into a real `ModelRouter.authorize_provider_call`
  invocation -- the exact worked example ADR-0050's own Decision
  section names. Four real tests prove it directly: a recalled
  `SENSITIVE` record correctly requires `Tier.CONFIRM` for a cloud
  provider (denied without confirmation, granted with); a `SECRET`-
  classified *task* merged with a `PUBLIC` record is denied
  unconditionally, even with full confirmation (proving the merge, not
  just the record's own classification, is what gates); a baseline
  `PUBLIC`+`PUBLIC` case still requires `CONFIRM` (no ALLOW-tier cloud
  egress exists in the fixed taxonomy); and a direct assertion that the
  resulting invocation's own provenance is the real merge, not a fresh
  `Provenance.user()`/`.system()` wrap. No new `Effect`/`Tier` decision
  was needed (ADR-0050's own Consequences section already said none
  would be) -- this closes an already-Accepted decision's own missing
  implementation, not a new safety tradeoff.
  `tests/meta/test_memory_provenance_carryforward.py`'s own AST scan
  needed no new allowlist entry: the new module only reads
  `record.value.provenance` (an attribute access) and never
  constructs a fresh `Provenance`, the exact "safe, encouraged
  pattern" that meta-test's own docstring already describes.
- ~~Only `str`-valued memories are supported.~~ **Narrowed 2026-09-02
  (overnight Track 5 pass), not fully removed.**
  `SqliteMemoryAdapter.write()` now persists any JSON-serializable
  value (`str`, `int`, `float`, `bool`, `None`, `list`, `dict`),
  raising `UnsupportedMemoryValueError` only for a genuinely
  non-serializable value (a custom object). Investigated first, per
  this pass's own instruction, whether this needed a new
  classification/effect decision: it did not.
  `application/memory/classification.py::memory_effect_for` already
  resolved `Effect` purely from `value.provenance.classification`,
  never from `type(value.value)`, and
  `MemoryWriteAuthorizer.authorize_write` was already generic over
  `T` -- neither changed. The one real, mechanical problem this pass
  solved: the chosen embedding pipeline (`EmbeddingPort`) only ever
  accepts text, so a non-`str` value is now embedded via its own
  `json.dumps` representation rather than the value itself (a real,
  stated precision loss for semantic search over structured data, not
  a safety concern). A new `value_json` column holds each new
  record's real `json.dumps` value, migrated into any pre-existing
  database via `ALTER TABLE ... ADD COLUMN` on open (checked with
  `PRAGMA table_info`, not exception-driven); a pre-migration row
  (`value_json IS NULL`) still reads back exactly as before, from the
  unchanged `text` column alone -- real backward compatibility, not a
  schema break, proven by a dedicated test that opens a hand-built
  pre-Track-5 database and confirms the column gets added, plus a
  second test that inserts a legacy NULL-`value_json` row directly and
  confirms it still reads back correctly. Seven parametrized round-trip
  tests (`int`, `float`, `bool` x2, `None`, `list`, `dict`) prove
  every JSON-serializable type is written, retrieved, and comes back
  as its own real Python type, not a stringified approximation. A
  real, stated limitation of using `json`, not silently hidden:
  round-tripping a `tuple` returns a `list` (JSON has no tuple type),
  and NaN/Infinity floats serialize via Python's own non-standard
  `allow_nan=True` default -- both are plain data-fidelity notes, not
  new gaps this pass introduced or needs to close. What remains open,
  not addressed by this pass: no real caller anywhere in this codebase
  (CLI, voice, or otherwise) actually writes a non-`str` memory today
  -- `kernel/memory.py`'s `authorize_and_remember` and every real
  entry point still only ever pass a `str`; this pass closed the
  storage-layer limitation, not added a new way to reach it.
- **No CLI subcommand exists for any memory capability.**
  `jarvis.cli.main` was not touched -- mirroring M3's own
  `docker.*`/`git.*` precedent (real, tested kernel functions with no
  CLI wiring), not an oversight specific to memory.
- ~~Voice-triggered recall does not exist.~~ **Closed 2026-09-02**
  (`feat: overnight Track 3`). `kernel/intent.py`'s grammar gained a
  real "recall `<query>`" command, mirroring "remember"'s/"read"'s
  identical shape (everything after the keyword, verbatim, becomes the
  query) -- resolving to `kernel.capabilities.MEMORY_RETRIEVE_CAPABILITY_ID`,
  a real, already-registered static capability (unlike "remember"'s
  dynamic-effect `memory.write`). `kernel/voice_loop.py`'s own dispatch
  gained the matching branch: a granted recall speaks back up to three
  recalled records' own real content, or a real, honest "I don't
  remember anything about that" when none match -- never silent.
  Eight real tests prove the full, real chain: intent resolution
  (query extraction, case-insensitivity, empty-query rejection), and
  end-to-end through `run_voice_loop` (a "remember" utterance followed
  by a "recall" utterance in the same run speaks the remembered
  content back; an empty store speaks the honest "nothing" answer; a
  **denied** physical confirmation still recalls successfully, proven
  directly against a real stored record -- `memory.retrieve`'s own
  `Tier.ALLOW` floor was never gated by the spoken answer at all,
  confirmed end-to-end through voice, not just at the
  `kernel/memory.py` composition-function level; and the confirmation
  prompt names the real query text, not a generic label). No new
  `Effect`/`Tier` decision -- `memory.retrieve`'s own authorization was
  already real and tested; this closes the real, missing voice
  grammar/dispatch wiring around it.
- **The real embedding model's correctness was checked against a
  five-to-seven-item, hand-built eval set, once.** This is real
  evidence the pipeline is not badly broken; it is not evidence of
  real-world retrieval quality at the scale or diversity of an actual
  user's own memorized facts over real time.
- **No numeric limit exists on how much a single memory-store SQLite
  file can grow**, beyond whatever this machine's own disk eventually
  enforces -- the same class of gap ADR-0044's own "no numeric resource
  limits" note already named for `SandboxPort`, restated here for a
  different real subsystem.

None of this is treated as blocking: this milestone's own acceptance
criteria (`m4-memory-retrieval.md`) already named several of these as
explicit, deferred, or open questions rather than promises, and this
closeout records what was actually built and verified rather than
rounding any of it up to "done." The one real exception, named above
rather than folded into this list, is ADR-0054's own acceptance --
that is a process deviation from this project's own governing rule,
not a scoped-out feature, and deserves the user's own direct attention
first.

## Milestone 5 additions

Appended per the same convention `docs/threat-model/v0.md`'s own
Milestone 1/2/3/4 sections established: real content reflecting what
was actually built (WP-67 through WP-75), not a rewrite of the M0–M4
snapshot above. New facts only.

### What was actually built

**Browser automation (WP-67 through WP-69), live-verified, not just
asserted from CDP's own documented capabilities**:
`CdpBrowserAutomationAdapter` (a hand-written CDP JSON-RPC client over
`websockets`, chosen after real evaluation against `python-cdp`/
`PyCDP`/`pychrome`/Playwright's `connect_over_cdp()`) launches a fresh,
dedicated, headless Brave instance per `open_page()` call, never the
user's own visible window. `test_real_cdp_flow_against_a_local_page`
ran for real on the development machine: a real headless Brave
instance, a real captured PNG screenshot (verified real PNG magic
bytes), and a real live-DOM query returning the exact expected outer
HTML -- satisfying `m5-browser-coding.md`'s own acceptance criterion 6
in full, not partially. Skipif-guarded on the real `brave-browser`
binary's presence; this project's own CI runner does not install it,
confirmed via `.github/workflows/ci.yml`, so this real test is
honestly skipped there, matching `test_sandbox.py`'s own precedent.
`browser.open_page`/`browser.screenshot`/`browser.inspect_dom`/
`browser.close_page` are real, registered, tested capabilities
(`kernel/browser.py`) -- `browser.close_page` itself, and the two real
process/profile-directory leak bugs its own real, live test found and
fixed (an unawaited SIGTERM leaving a zombie process; a Chromium
child process outliving a single-pid SIGKILL), are recorded in that
work's own commit, not repeated here.

**The coding-agent authorization chain (WP-70 through WP-73),
end-to-end, real and proven**: `Effect.CODE_WRITE`/
`Effect.PROTECTED_PATH_WRITE` and `code_write_effect_for` (WP-70,
ADR-0056) are the real classification mechanism, with a real,
fail-closed detector for a target repository's own test convention
(pytest/Go/RSpec/Jest-family signals, researched live, not guessed)
and a real `UnrecognizedTestConventionError` refusal when none is
detected and no explicit override is given -- never a silent
Python-pattern default on a non-Python repository. A real,
AST-based structural meta-test
(`tests/meta/test_workspace_apply_patch_single_path.py`) proves
`WorkspacePort.apply_patch` is referenced nowhere under `src/jarvis`
outside a real, named allowlist. `make_disposable_workspace` (WP-73)
gives every real `Dispatcher.run()` climb a fresh,
`SandboxPort`-contained filesystem copy of the target repository,
proven directly: a real `Dispatcher`/`EscalationLadder`/`Arbiter`/
`ModelRouter`/`PytestValidator` chain, given a real failing test and a
real patch, was run end-to-end, and the real target repository's own
file was checked, directly against the filesystem, to be byte-for-byte
unchanged afterward. `run_coding_task` (WP-71) ties both together: a
real, finite retry budget (`DEFAULT_MAX_CLIMBS = 3`, justified, not
arbitrary) that provably stops rather than looping forever; an
all-or-nothing real-write rule proven by a real test (a winning patch
touching one ordinary and one protected path is rejected wholesale,
neither file written); and a real, live, end-to-end proof that the
wrapper retries across two full `Dispatcher.run()` climbs and performs
exactly one real, authorized write to the real target repository.
`kernel/coding.py` (WP-72) registers `coding.run_task`
(`Effect.EXECUTE`, `Tier.CONFIRM`) as the outer gate on invoking the
coding agent at all, deliberately separate from the per-write
authorization inside `run_coding_task` itself -- proven by a real test
showing a denied outer gate never invokes the coding agent even once
(a call-tracked fake `ReasoningPort`'s own call count stays at zero).

**The Console UI mechanism (WP-74)**: `ConsolePort`/`GtkConsoleAdapter`/
`jarvis.ui.console.show_console_line` are real -- a fresh, detached
GTK4 subprocess per line, never blocking the caller, `text`/`timeout_s`
passed as real subprocess arguments, never interpolated into the
launched script's own source. Wired into exactly one real call site,
`kernel/browser.py`'s `authorize_and_open_page`: a granted, successful
open shows a real on-screen line, proven by a real test asserting the
exact text shown; a denied decision shows nothing, proven by a
matching real test. No specific views were designed, per the recovered
fragment's own explicit instruction, quoted in full in
`m5-browser-coding.md` itself.

### ADR-0055 and ADR-0056 were accepted by relayed instruction, not by the user's own independent reading -- restated plainly, one more time

Both ADRs were drafted from remotely-reasoned working assumptions, the
same "not confirmed by the user directly in conversation" position
`m5-browser-coding.md`'s own header states for this entire milestone,
unlike M0 through M4's own ADRs. Their eventual acceptance (recorded in
each ADR's own "Accepted 2026-09-01" note) followed a real, stated bar
the user set *in this same conversation*: build WP-70/WP-73/WP-71,
prove each one's own required safety property with real, passing
tests, only then accept. That bar was met and checked directly before
acceptance -- but the acceptance itself was this session's own
judgment that the bar had been met, acting on the user's own relayed
instruction to accept once it had, **not the user's own independent
reading of the final ADR text, the way M4's own ADR-0048 through
ADR-0053 were each accepted directly, by the user, in conversation**.
This distinction was stated plainly to the user at the time and is
restated here, in the closeout record, for the same reason: it should
not quietly read as "user-reviewed" in the way M4's closeout could
honestly say. If the user's own eventual reading of ADR-0055/ADR-0056
differs from this session's judgment that WP-70/71/73 satisfied their
own required bar, these are the two documents in this milestone most
likely to need reopening.

### Real, deliberate scope limits and gaps this milestone does not close

Named plainly, mirroring M1/M2/M3/M4's own closeout discipline --
nothing below is rounded up to "done":

- **`coding.run_task` has no default `dispatcher_factory`, and
  therefore no real caller anywhere in this codebase.** Deliberate,
  not an oversight (see `kernel/coding.py`'s own docstring): which
  real `ReasoningPort` providers service `SELF_REPAIR`/
  `SECOND_PROVIDER` was never decided by any real ADR in this project,
  and choosing a default cloud-provider assignment (vendor-family
  adapter, model, keyring secret reference) here would have been new,
  undecided policy this work package was not asked to make.
  `_local_only_dispatcher_factory` is a real, credential-free option
  any future caller may reuse, but nothing in this codebase actually
  calls it yet -- the coding agent is authorizable and its own safety
  mechanism is fully proven, but it has no real, wired entry point
  (CLI, voice, or otherwise) a person could actually use today.
- ~~**No voice grammar exists for `coding.run_task`.**~~ **Closed
  2026-09-02 (overnight Track 4 pass).** Checked directly against
  `kernel/intent.py` before this decision, not assumed: no established
  precedent existed to mirror at the time this gap was written.
  `memory.write` remained the one real exception (ADR-0053's own
  explicit commitment); `browser.*` (all four WP-67-69 capabilities)
  and M4's `memory.pin`/`memory.forget` were all left kernel-level
  only, and `coding.run_task` had followed that larger, established
  precedent, not a new one invented for this milestone -- **a real,
  deliberate departure made explicitly for this pass, not a reversal
  of the precedent itself**: those other capabilities' own gaps remain
  open because no one asked for voice wiring; this one was closed
  because the overnight prompt explicitly named it as Track 4's own
  scope. `kernel/intent.py` gained a "code `<task>`" command
  (mirroring "remember"/"recall"'s exact "keyword + rest of text"
  shape) resolving to the already-registered, static
  `coding.run_task` capability. `kernel/voice_loop.py`'s
  `_authorize_and_execute` became `async` (the one composition function
  in its dispatch table that is itself `async`) and gained a
  `CODING_RUN_TASK_CAPABILITY_ID` branch. `run_voice_loop` gained two
  new optional parameters, `coding_target_repo`/`coding_dispatcher_factory`
  -- pass-throughs to `authorize_and_run_coding_task`'s own existing
  required parameters, which still have no safe default (see the gap
  immediately above, still open) -- with a real, honest "Coding isn't
  configured on this device." spoken fallback when either is `None`
  at the time a "code" command resolves, never a crash, never a silent
  no-op. This closes only the voice-grammar gap, not the
  no-default-`dispatcher_factory` gap above, which remains open exactly
  as stated. No new Effect/Tier decision was made: `coding.run_task`'s
  own outer `Effect.EXECUTE`/`Tier.CONFIRM` gate (WP-72) still requires
  confirmation before anything runs, voice-triggered or not -- proven
  directly in `tests/unit/test_voice_loop.py` (a denied "code" command
  never invokes the coding agent, mirroring `test_coding_kernel.py`'s
  own `provider.call_count == 0` proof, now exercised end-to-end
  through voice).
- **`Dispatcher`'s own multi-candidate-per-rung accumulation, without
  revert, remains a real, pre-existing gap -- contained, not
  resolved.** Multiple candidates tried at one rung (the real
  `SECOND_PROVIDER` default tries two providers) are each applied to
  the *same* workspace in sequence, with no revert between them --
  named in ADR-0043's own Consequences section as deferred
  "workspace lifecycle management" before M5 ever existed, and never
  actually closed by WP-37's own `Dispatcher`. WP-73's disposable
  workspace ensures this accumulation only ever happens inside a copy
  discarded after every climb, never the real repository -- a real,
  complete fix for *this milestone's own* safety property, but not a
  fix for the underlying `Dispatcher`/`ValidationPort` lifecycle
  question itself, which would require modifying `Dispatcher`,
  something ADR-0055's own Decision explicitly declines to do.
- **`browser.close_page` closes the authorization gap, not the
  automatic-invocation gap.** Named at WP-67-69's own closeout and
  still true: nothing in this codebase calls it automatically after a
  `browser.screenshot`/`browser.inspect_dom` call, or on any idle
  timeout. A caller must still invoke it explicitly, the same as
  `docker.stop_container` -- a deliberate, consistent choice, not a
  shortcut unique to this capability.
- **The Console UI mechanism's own real GTK4-subprocess launch path
  (`_default_launch`/`_default_show_line_fn`) has no automated test.**
  The same class of deliberate gap `jarvis.ui.confirm.dialog`'s own
  `show_confirmation_dialog` already has: a real display is required,
  CI is headless, and this project's own established precedent
  (`test_sandbox.py`'s real-GUI test, `adapters/browser_automation.py`'s
  real-CDP test) is to prove real display-dependent code by direct,
  real, manual verification rather than force it into the automated
  suite. Only `browser.open_page` shows a real console line today --
  `coding.run_task` and every other real capability in this codebase
  remain spoken-only (via `TtsPort`, unmodified, wherever they are
  already wired to it) or entirely silent on-screen; wiring more
  capabilities through `ConsolePort` is real, future work this pass's
  own "at least one real action" scope did not require.
- **LSP-based code intelligence -- the "coding capabilities via LSP +
  git" half of this milestone's own original working assumption --
  was never answered or built.** `m5-scoping-notes.md`'s own real,
  live research (Part 2) found a real, viable candidate library
  (`pygls`'s own sans-IO client-side alternative, not `pygls` itself)
  but the milestone-scoping question that research fed into -- whether
  M5 should design and build an LSP-backed `CodeIntelligencePort`
  alongside the escalation-ladder-based coding-loop wrapper this
  milestone actually built, or defer it entirely -- was never posed to
  the user and never resolved. This closeout does not resolve it
  either, by omission or otherwise: it is real, unresolved scope,
  exactly as `m5-browser-coding.md`'s own "Real gaps in the given
  assumptions" section already named it, carried forward unchanged,
  not quietly dropped now that the rest of the milestone is
  code-complete.
- **The coding-loop wrapper's own retry-budget shape, deliberately
  left open by ADR-0055's own Decision, was resolved for real during
  implementation** (`DEFAULT_MAX_CLIMBS = 3`, a plain integer
  parameter, justified in `application/coding/loop.py`'s own
  docstring) -- named here only to close out
  `m5-browser-coding.md`'s own acceptance-criteria caveat that this
  was undecided; it no longer is, and future work does not need to
  revisit it as an open question.
- **Console UI views remain deliberately undesigned**, exactly as the
  recovered fragment instructed -- not a gap, the intended, permanent
  shape of this deliverable until real use over time reveals what a
  view should actually look like.

None of this is treated as blocking: `m5-browser-coding.md`'s own
acceptance criteria and "Incomplete, stated plainly" section already
named several of these as explicit, deferred, or open questions rather
than promises, and this closeout records what was actually built and
verified rather than rounding any of it up to "done." The one real
exception, named above rather than folded into this list, is the
ADR-0055/ADR-0056 acceptance note -- read that section first if only
reading one.

## Milestone 6b additions

Appended per the same convention `docs/threat-model/v0.md`'s own
Milestone 1/2/3/4/5 sections established: real content reflecting what
was actually built (WP-82 through WP-85, implementing
`docs/architecture/m6b-job-assistance.md` and ADR-0058, both Accepted
2026-09-02), not a rewrite of the M0–M5 snapshot above. New facts
only. M6a is entirely untouched by this particular pass (its own
"Milestone 6a additions" section below was written by a later pass,
once its own read-only implementation existed).

### What was actually built

**The structural meta-test landed first, proven against an empty
package, before any real capability code existed** --
`tests/meta/test_job_assistance_no_submission.py`, mirroring
`test_no_response_scraping.py`'s/`test_terminal_sandboxed_launch_only.py`'s
own established AST-scan precedent. Real, mechanical proof of
ADR-0058's own Decision, not prose alone: no raw HTTP-client identifier
(`requests`/`httpx`/`aiohttp`/`urlopen`/`urlretrieve`/`Request`/`post`)
and no unlisted `BrowserAutomationPort`-shaped method call
(`submit_form`/`click`/`fill`/`fill_form`/`dispatch_form_submit`/
`press_key`/`type_text` -- none exist on the real port today, named as
a future-bypass-risk check mirroring ADR-0057's own finding 4) may ever
appear in real code under `application/job_assistance/`,
`kernel/job_assistance.py`, `ports/draft_storage.py`, or
`adapters/draft_storage.py`; no function/method in that scope may
declare a submission-shaped parameter name (`submit`/`apply_to`/
`application_payload`/`credentials_for_submission`). Each assertion
carries its own real "fires on a deliberate violation" proof (an
inline snippet importing `requests` and calling `.post()`; another
calling `browser.submit_form()`; another declaring an
`application_payload` parameter) and a real false-positive check (a
legitimate `browser.open_page()`/`query_dom()` call, an ordinary
drafting-capability signature -- neither ever flagged). Re-run and
re-confirmed clean after every subsequent work package (WP-82 through
WP-85) added real code, not just once at the start.

**`DraftStoragePort`/`LocalDraftStorageAdapter` (WP-82/WP-83)**: one
new, minimal write port (`save(filename_hint, content) -> Path`) --
checked against `WorkspacePort` first and rejected (`apply_patch` is
`git apply`-backed, expects unified-diff text against an existing
repository; a drafted cover letter is plain prose with no repository
to diff against, a real, honest mismatch, not forced into that shape).
A real, defensive finding made during implementation, not named in
the design doc itself: `filename_hint` is caller-supplied text, not a
validated filename -- sanitized to alphanumeric/dash/underscore only
*before* it ever touches a real path, so a path separator or `..`
segment cannot survive into the constructed path at all, proven by
real tests injecting both `../../../../tmp/escaped` and a bare `/`
path segment. Never overwrites an existing file (a real, incrementing
numeric suffix), proven across three consecutive saves with the same
hint.

**`job_assistance.draft` (WP-84)**: the first real, invocable M6b
capability. Reuses `application/reasoning/unverifiable.py`'s
`UnverifiableTaskHandler` (deliverable #7, ADR-0040, already built for
M2) unmodified -- checked against `Dispatcher` first and rejected: a
cover letter has no pass/fail validator, so the escalation-ladder/
self-repair machinery built for verifiable tasks does not fit;
`UnverifiableTaskHandler`'s own real shape (every authorized provider
asked once, in parallel, a human picks via
`CandidatePresentationPort`) is a precise, structural match instead.
Every real provider call it makes already routes through
`ModelRouter.authorize_provider_call` -- the same
`Effect.EGRESS_SENSITIVE`/`Effect.EGRESS_SECRET` gate every other
reasoning-provider call in this codebase already uses; no new
`Effect`/`Tier` decision was needed for that part. No outer,
`coding.run_task`-style `Effect.EXECUTE`/`Tier.CONFIRM` gate exists --
checked and rejected as unnecessary, not merely omitted: drafting has
no per-write inner loop the way the coding agent does, so
`DraftWriteAuthorizer`'s own single `Decision` is both the first and
only real gate this capability needs. Real tests prove: a granted
invocation actually invokes every configured provider and saves the
human-selected candidate's own content exactly once; a denied
invocation never invokes any provider or the storage port at all
(mirroring `test_coding_kernel.py`'s own `provider.call_count == 0`
proof); remote confirmation alone suffices (the ordinary `CONFIRM`
floor); a granted invocation appends a real, verifiable audit record.
**Console UI wired (post-WP-86)**: a granted draft shows a real
on-screen line via `ConsolePort.show_line`, mirroring
`authorize_and_open_page`'s own identical mechanism exactly, never
called on a denied decision -- see "Real, deliberate scope limits"
below for the closed-gap record.

**Research (WP-85) needed no new code at all** -- checked against
M6a's own item-6 resolution, not assumed to match: reading a job
posting is mechanically identical to reading any other real web page,
so the identical conclusion holds. Two real proofs: a structural test
that `application/job_assistance/` contains no research-orchestration
module (`__init__.py`/`classification.py`/`drafting.py` only, nothing
else), and a behavioral test that a direct call to
`kernel.browser.authorize_and_open_page` -- the real, already-Accepted
action a future job-posting-research flow takes -- calls only
`open_page` on the injected `BrowserAutomationPort`, never
`query_dom`/`capture_screenshot`/`close`.

### The SECRET-input default is now the user's own confirmed, permanent decision (2026-09-02)

`m6b-job-assistance.md`'s own design left one question genuinely open:
whether `Classification.SECRET` drafting input deserves
`Effect.MEMORY_WRITE`'s own unconditional-`DENY` floor (the same
protection memory writes already get, ADR-0049) or the ordinary
`Effect.WRITE_LOCAL`/`Tier.CONFIRM` floor the design doc's own
static-capability sketch first assumed. Implementing
`application/job_assistance/classification.py::draft_effect_for` for
real forced a concrete choice regardless -- some real behavior must
exist the first time real `SECRET` content is passed as drafting
input; leaving the function unwritten was not an option once real code
needed to exist. **The conservative choice made: `SECRET` reuses
`Effect.MEMORY_WRITE`'s own `DENY` floor** -- a real reuse of that
existing enum member's tier-floor *behavior*, not a claim that a
drafted document conceptually *is* a memory write, and deliberately
not a new, permanent domain-taxonomy addition (`Effect.CODE_WRITE`/
`Effect.PROTECTED_PATH_WRITE`'s own precedent for "new effect when the
write shape is genuinely new" was considered and set aside here in
favor of the smallest, most reversible option: a future ADR settling
this question for real can freely swap which effect this one function
returns without touching `domain/capability.py` at all). **This also
makes `job_assistance.draft` a dynamic-effect capability, not the
static one the design doc first sketched** -- deliberately not
registered in `build_default_registry()`, mirroring `memory.write`'s
own identical reasoning. A real property test
(`tests/property/test_drafting_writer.py`) proves the conservative
choice holds under every real `PolicyContext`, including full physical
confirmation, mirroring ADR-0038's/ADR-0049's own required rigor for
every other unconditional-`DENY` claim in this codebase.

**Confirmed by the user directly, in conversation, 2026-09-02: keep
the unconditional `Effect.MEMORY_WRITE`-style `DENY` floor for
`SECRET` drafting input, no override, matching memory-write's own
precedent exactly.** This is no longer a provisional implementation
default awaiting review -- it is the real, permanent policy, the same
way ADR-0058's own acceptance record turned a relayed decision into a
directly-confirmed one. No code changed as a result: `draft_effect_for`'s
own real behavior was already correct; only this document's own
"flagged, not yet decided" framing is retired.

### What the structural meta-test actually proves, and what it genuinely cannot

Named plainly, matching this project's own "state real limits, don't
imply more than what was checked" discipline: the meta-test proves
that no real code path under M6b's own package boundary can call a
raw HTTP client, an unlisted `BrowserAutomationPort` method, or declare
a submission-shaped function parameter -- a real, mechanical guarantee
about *this codebase's own source text*, re-verified on every gate run.
**It cannot, and does not claim to, catch**:

- **A reasoning provider itself being coerced, via prompt injection or
  otherwise, into producing content that *describes* or *encodes* a
  submission action** (e.g. a drafted "cover letter" that is actually
  adversarial instructions aimed at some other, future system that
  might read it) -- the meta-test only inspects this repository's own
  Python source; it has no visibility into, and cannot constrain, what
  a cloud or local model *generates* as `Candidate.content`. The
  structural guarantee is that *this codebase* never submits anything
  regardless of what a provider outputs -- a real, different, and
  weaker (but still real) property than "no adversarial content can
  ever be generated."
- **A future dependency (a new third-party package) that reaches the
  network via a mechanism this ban-list does not name** -- the
  identifier ban-list is a fixed, named set (`requests`/`httpx`/
  `aiohttp`/`urlopen`/`urlretrieve`/`Request`/`post`), not an
  exhaustive enumeration of every possible way Python code can reach
  the network. A new library imported under a different top-level name
  that itself performs an HTTP POST internally would not be caught by
  identifier-scanning its *caller's* source alone.
- **Anything outside M6b's own scanned package path** -- if a future,
  unrelated capability elsewhere in this codebase were extended to
  accept a job-assistance `Candidate` and submit it somewhere, this
  meta-test would never see it; the boundary is scoped to
  `application/job_assistance/`, `kernel/job_assistance.py`,
  `ports/draft_storage.py`, and `adapters/draft_storage.py` only, by
  design (ADR-0058's own Decision names this exact scope).

None of this weakens ADR-0058's own real guarantee for what it actually
covers -- this codebase's own M6b code path genuinely has no submission
mechanism, mechanically re-verified, not merely documented. It is
named here so that guarantee is never overstated beyond what was
actually checked.

### Real, deliberate scope limits this milestone does not close

- **No real caller anywhere in this codebase invokes
  `authorize_and_draft_document` yet** -- no CLI subcommand, no voice
  grammar, matching `coding.run_task`'s own identical "authorizable and
  proven, but no wired entry point a person could actually use today"
  gap. `providers` has no default (mirrors `coding.run_task`'s own
  `dispatcher_factory` precedent exactly) -- which real `ReasoningPort`
  providers draft a real cover letter was never decided by any ADR.
- ~~The SECRET-input conservative default is real, implemented, and
  tested -- but not yet confirmed by the user as the right long-term
  choice.~~ **Closed 2026-09-02**: confirmed directly by the user, in
  conversation -- keep the unconditional `DENY` floor, no override.
  See this section's own "SECRET-input default" note above for the
  full record.
- **The structural meta-test's own real, named limits (above)** --
  provider-generated content is never inspected for adversarial intent,
  the identifier ban-list is a fixed, named set rather than an
  exhaustive network-reachability proof, and the scan boundary is
  M6b's own package path only.
- ~~No console-line wiring for `job_assistance.draft`.~~ **Closed
  2026-09-02**: `authorize_and_draft_document` gained a `console`
  parameter and shows a real, on-screen `"job_assistance.draft: {task}"`
  line via `ConsolePort.show_line` on a granted draft, mirroring
  `kernel/browser.py`'s own `authorize_and_open_page` exactly, same
  shape, no new mechanism. Never called on a denied decision, proven
  directly (`test_denied_draft_never_shows_a_console_line`, mirroring
  `authorize_and_open_page`'s own identical
  `test_denied_open_page_never_shows_a_console_line`). M6a's own
  identical deferral remains open -- unrelated to this closure, M6a is
  still unimplemented.
- **`m6-scoping-notes.md`'s own "job-search sourcing" question remains
  open** -- which real, vendor-neutral job-listing source(s) a future
  research flow would actually use was never decided; this milestone's
  own research boundary test proves the *mechanism* (`browser.open_page`
  only) is safe, not that a real source has been chosen.

None of this is treated as blocking: `m6b-job-assistance.md`'s own
acceptance criteria and "Incomplete, stated plainly" section already
named several of these as explicit, deferred, or open questions rather
than promises, and this closeout records what was actually built and
verified rather than rounding any of it up to "done." Not tagged --
tagging remains a separate, later action, mirroring every prior
milestone's own precedent.

## Milestone 6a additions

Appended per the same convention this file's own Milestone 1/2/3/4/5/6b
sections established: real content reflecting what was actually built
(WP-76 through WP-80, the read-only half of `m6a-communications.md`),
not a rewrite of the M0–M6b snapshot above. New facts only. Added
after Milestone 6b's own section here because that is the real,
chronological order these two milestones were actually built in this
repo -- M6a's own design predates M6b's, but M6b's own implementation
landed first.

### The one hard gate this pass respected throughout

**ADR-0057 remains `Proposed`, not `Accepted` -- unreviewed by the
user directly.** Nothing whose real `Effect`/`Tier` classification
depends on that ADR's own reasoning was implemented: `EmailPort.send_message`
and `CalendarPort.create_event` exist on their own Protocols (for
`EmailPort`'s/`CalendarPort`'s own conceptual completeness -- one port
per real capability, the same shape `BrowserAutomationPort` already
bundles multiple real CDP domains behind), but every real adapter's
own implementation unconditionally raises `NotImplementedError`,
before any real SMTP/CalDAV-write call. Proven structurally, not
merely by inspection today: `tests/meta/test_communications_no_send_or_create.py`,
mirroring `tests/meta/test_job_assistance_no_submission.py`'s own
"prove the boundary, don't just assert it" discipline. Real,
confirmed-current `caldav` 3.2.1 write-method names (`add_event`,
`save_event`, `save_with_invites`, `save_object`, `add_object`) are
banned from appearing anywhere in M6a's own real scope, alongside
`smtplib` -- not hypothetical names, checked directly against the
installed library before writing the ban list. `application/communications/classification.py`
(WP-79, the dynamic-effect classification function ADR-0057 would
govern) does not exist in this codebase.

### What was actually built

**`EmailPort`/`CalendarPort` (WP-76)**: `domain/email.py`
(`EmailSummary`/`EmailMessage`), `domain/calendar.py`
(`CalendarEvent`/`CalendarEventDraft`), `ports/email.py`,
`ports/calendar.py` -- per `m6a-communications.md`'s own already-designed
shapes. Contract tests against fakes only at this stage, matching
`BrowserAutomationPort`/`SandboxPort`/`MemoryWritePort`'s own
established ordering.

**`ImapEmailAdapter`/`CalDavCalendarAdapter` (WP-77/WP-78)**: real,
read-only implementations. `imaplib` (standard library) for
`list_messages`/`read_message`; the `caldav` library (a real, new
dependency, added and evaluated for real -- 3.2.1) for `list_events`.
Both wrap their own real, blocking client calls in `asyncio.to_thread`
-- a new pattern in this codebase, since neither `imaplib` nor
`caldav`'s synchronous client has an async twin in this project's own
existing dependency set. **Real API quirks found and worked around,
confirmed via `mypy --strict`'s own `reveal_type` against real code,
not assumed from documentation**: `caldav`'s top-level package uses
PEP 562 lazy `__getattr__` for its own re-exports, which defeats
mypy's static resolution entirely (`caldav.DAVClient` resolves to
`object`, not a class, under strict mode) -- imported from its real
submodule instead (`caldav.davclient.DAVClient`). `DAVClient.principal()`
(the method the library's own documentation shows in most examples) is
itself unannotated; `DAVClient.get_principal()` is the real, typed
replacement the library's own docstring already names as the
"for new code" choice -- used here instead, closing a real
`mypy --strict` gap the more obvious call would have left open.
`Principal.calendars()`/`Calendar.date_search()` both really return
`X | Coroutine[Any, Any, X]` (one shared method serving both the sync
and async client) -- narrowed with a real `isinstance(calendars, list)`
check, never cast away. `icalendar`'s own `ATTENDEE` property real
shape (`None` for none, a bare `vCalAddress` for exactly one, a real
`list` for more than one) was confirmed by direct construction against
the installed library before the parser was written, not assumed.

**`communications.list_email`/`communications.read_email`/
`communications.list_calendar_events` (WP-80)**: three new, real,
invocable, static capabilities (`Effect.EGRESS_LOCAL`, `Tier.ALLOW`
-- the identical shape `fs.read_file`/`memory.retrieve`/
`browser.screenshot` already establish), registered in
`build_default_registry()`. Real, adversary-influenced content is
tainted at the point it enters this codebase, per-item (not one
blended-source tuple across a whole result set): every returned
`EmailSummary`/`EmailMessage`/`CalendarEvent` individually wrapped as
`Tainted(value, Provenance.external(source=<its own real id>,
classification=Classification.SENSITIVE))`, mirroring
`authorize_and_capture_screenshot`'s own real tainting exactly --
anyone who can email the user, or invite them to a calendar event, can
put content in front of JARVIS, the same real threat class
`browser.screenshot`'s own docstring already names for web content.
Real tests prove: `EGRESS_LOCAL`/`Tier.ALLOW` means even a fully
denied confirmation still reads real content (mirroring
`memory.retrieve`'s own established "denied confirmation still
succeeds" test pattern) -- **a real correction made during this pass,
named plainly**: the first draft of these tests wrongly asserted a
denied confirmation blocks the read, an incorrect assumption caught by
the tests actually failing against the real `AuthorizationOrchestrator`
before commit, not shipped and only found later.

**A real, deliberately conservative scoping choice, beyond what the
design doc itself required, stated explicitly rather than left
implicit**: `m6a-communications.md` classifies an attendee-less
`CalendarPort.create_event` as ordinary `Effect.WRITE_LOCAL`/`Tier.CONFIRM`
-- not gated by ADR-0057 at all, and so, in principle, implementable
today without waiting for that ADR's review. This pass implements
neither case, attendee-less or not: `create_event` raises
`NotImplementedError` unconditionally for every real draft, so there
is exactly one clean, unambiguous "everything past reading needs
ADR-0057" boundary in this codebase, not a partially-implemented write
path whose own scope would itself need separate, later explanation.

### Real, deliberate scope limits this milestone does not close

- **The entire write half remains unbuilt, by design, blocked on
  ADR-0057** -- `EmailPort.send_message`, `CalendarPort.create_event`
  (including the attendee-less case, per this pass's own conservative
  scoping choice above), and `application/communications/classification.py`'s
  own dynamic-effect authorizers (WP-79) all remain real, separate,
  future work once the user reviews ADR-0057 directly.
- **No real caller anywhere in this codebase invokes
  `authorize_and_list_email`/`authorize_and_read_email`/
  `authorize_and_list_calendar_events` yet** -- no CLI subcommand, no
  voice grammar, matching `coding.run_task`'s/`job_assistance.draft`'s
  own identical "authorizable and proven, but no wired entry point a
  person could actually use today" gap. `email_port`/`calendar_port`
  have no default (mirrors `job_assistance.draft`'s own `providers`
  precedent exactly) -- real, per-deployment IMAP host/CalDAV URL/
  credentials were never this module's decision to make.
- **No real, live test against a real mailbox/calendar account
  exists** -- `m6a-communications.md`'s own acceptance criterion 6
  names this explicitly; no real test-account IMAP/SMTP/CalDAV
  credentials are configured anywhere in this environment, matching
  `test_real_cdp_flow_against_a_local_page`'s own established
  precedent for network-dependent adapters. Both real adapters' own
  unit tests are hermetic, against fakes, proven correct at the
  parsing/wiring level, not against a real server.
- **The `caldav` library's own real API surface was evaluated only as
  deeply as this pass's own real, read-only need required** -- a
  fuller evaluation against alternative CalDAV client libraries
  (`m6a-communications.md`'s own "real, separate work" note) remains
  real, deferred work, not resolved here.
- **No console-line wiring for any M6a capability** -- matching
  `job_assistance.draft`'s own now-closed gap's *prior* state before
  it was closed; M6a's own three real capabilities have not had the
  same treatment applied in this pass, real, deferred implementation
  detail.

None of this is treated as blocking: `m6a-communications.md`'s own
acceptance criteria and "Incomplete, stated plainly" section already
named several of these as explicit, deferred, or open questions rather
than promises, and this closeout records what was actually built and
verified rather than rounding any of it up to "done." **The one real
exception, named above rather than folded into this list**: at the
time this section was written, ADR-0057 itself remained genuinely
unreviewed by the user -- the entire reason this pass's own scope
stopped where it did. That has since changed -- see "ADR-0057 Accepted
(2026-09-03)" immediately below. Not tagged -- tagging remains a
separate, later action, mirroring every prior milestone's own
precedent.

### ADR-0057 Accepted (2026-09-03)

**The hard gate named above has been lifted, by the user's own direct
action, not by this pass's own judgment.** The full, current text of
`docs/adr/0057-email-send-and-attended-calendar-events-reuse-egress-sensitive-egress-secret.md`
(Decision, Consequences, and the 2026-09-01 Amendment, all unchanged
from what this section describes above) was surfaced to the user
verbatim in conversation; the user reviewed it and responded "Accept
ADR-0057 as-is." This satisfies the same "reviewed the document, not
merely relayed a decision" bar ADR-0058 already met, and which
ADR-0055/ADR-0056 still have not -- see the ADR's own updated `##
Status` section for the equivalent record.

**What this does and does not change, stated precisely**: the
classification question ADR-0057 answers (`Effect.EGRESS_SENSITIVE`/
`Effect.EGRESS_SECRET`, reused, no new effect) is now settled. It does
not implement anything -- `EmailPort.send_message`/`CalendarPort.create_event`
still unconditionally raise `NotImplementedError` in every real
adapter as of this note, exactly as this section's own "What was
actually built" describes above; `application/communications/classification.py`
(WP-79) still does not exist. The scope limits named in the section
above this one remain real and open -- only their *cause* changes,
from "blocked on ADR review" to "not yet implemented," the identical
distinction this file already draws for `coding.run_task`'s
no-default-`dispatcher_factory` gap and `job_assistance.draft`'s
no-default-`providers` gap.

### M6a write half implemented (2026-09-03, WP-79 onward)

Built immediately after the Acceptance above, in the same pass, per
the user's own direct instruction ("implement the write half — WP-79
onward"). The hard gate this pass respected: ADR-0057 is now Accepted,
so classification is settled; nothing here reopens or reinterprets
that ADR's own Decision.

**`application/communications/classification.py`**: `egress_effect_for`
mirrors `application/reasoning/classification.py`'s own identical
function (a real, separate module, not a cross-package import,
matching `application/coding/classification.py`'s established
precedent) -- `Classification.SECRET` maps to `Effect.EGRESS_SECRET`
(unconditional `Tier.DENY`), everything else to `Effect.EGRESS_SENSITIVE`
(`Tier.CONFIRM`). `calendar_effect_for` adds one real branch on top:
`has_attendees=False` always floors `Effect.WRITE_LOCAL`/`Tier.CONFIRM`
regardless of classification (`git.push`'s own precedent), delegating
to `egress_effect_for` only when `has_attendees=True`.

**`application/communications/writer.py`**: `EmailSendAuthorizer.authorize_send`/
`CalendarEventAuthorizer.authorize_create`, mirroring
`MemoryWriteAuthorizer.authorize_write` exactly -- a fresh
`CapabilityDescriptor` per call, `AuthorizationOrchestrator.authorize()`
evaluates it, the real port call is strictly the caller's own
responsibility afterward, only if `Decision.granted`. Neither
`communications.send_email` nor `communications.create_calendar_event`
is registered in `build_default_registry()`, the same reason
`memory.write` never is -- the real effect genuinely varies per
invocation.

**Real adapter implementations**: `ImapEmailAdapter.send_message`
(`adapters/email.py`) now uses stdlib `smtplib.SMTP_SSL`, building a
real `email.message.EmailMessage` and sending via `SMTP_SSL.send_message`.
Kept its original class name rather than the design doc's own
`ImapSmtpEmailAdapter` sketch -- a real, deliberate, minimal-footprint
choice, named explicitly rather than silently deviating: renaming
would have touched every existing real caller/test for no functional
benefit. A new, required `smtp_host` constructor parameter was added
(no default -- most real providers use a genuinely different hostname
for SMTP than IMAP; assuming they match would be a real, silent
misconfiguration risk).

`CalDavCalendarAdapter.create_event` (`adapters/calendar.py`) uses
`caldav`'s real, current, non-deprecated `Calendar.add_event(dtstart,
dtend, summary, attendee=[...])` -- confirmed directly against the
installed 3.2.1 library (`save_event` is a real, documented deprecated
alias for it; `add_event`, not `save_with_invites`, was chosen since
this design has no `organizer` concept, and both real methods write
the identical ATTENDEE-bearing VEVENT a real CalDAV server's own RFC
6638 scheduling logic acts on regardless of which was used). Attendee
addresses are given the `mailto:` scheme prefix before being handed to
`icalendar`'s own `vCalAddress` encoding -- confirmed directly
(`icalendar.vCalAddress(...).to_ical()` does not add the prefix
itself), mirroring the read side's own `_strip_mailto` in reverse. A
real, checkable failure mode -- `caldav`'s own `.id` property is
genuinely typed `str | None` -- is handled by a new
`CalendarEventCreationError`, not papered over: a compliant server
always echoes back a real UID, so this is a real protocol-level
anomaly check, not a defensive "can't happen" guard. A second,
structurally-real-but-practically-unreachable branch (`add_event`'s
own return type is `Event | Coroutine[Any, Any, Event]`, since the
same real method also serves `caldav`'s async client) is narrowed with
a real `isinstance(created, Coroutine)` check, mirroring
`_list_events_sync`'s own established `isinstance(calendars, list)`
narrowing for the identical reason -- this adapter only ever
constructs a synchronous `DAVClient`.

**`kernel/communications.py`**: `authorize_and_send_email`/
`authorize_and_create_calendar_event` extend the existing composition
root, mirroring `authorize_and_remember`'s own shape exactly --
outgoing content (`body`/`summary`) is wrapped
`Tainted(value, Provenance.user())`, always `Classification.PUBLIC`
today, the identical trust-boundary caveat `authorize_and_remember`'s
own docstring already states, restated here verbatim: a future caller
constructing this value from a less-trusted or more sensitive source
is responsible for real, considered classification -- these functions
do not, and cannot, second-guess a provenance they did not compute.

**The structural meta-test that once proved this boundary was
retired, not left stale**: `tests/meta/test_communications_no_send_or_create.py`'s
entire invariant ("no send/create code path exists") is now
intentionally, deliberately violated by this pass's own real code --
leaving it in place would have meant either a permanently-failing gate
or a meta-test asserting the wrong thing. Deleted outright, its
retirement recorded here rather than silently dropped, mirroring this
project's own "report every deviation" discipline. Unlike ADR-0058's
own M6b submission boundary (a real, permanent, structural
prohibition the design doc itself calls out as needing a new ADR to
ever lift), ADR-0057's boundary was conditional on review from the
start -- once accepted, the block is gone by design, not weakened.

**Real tests, all passing**: `tests/unit/application/communications/test_classification.py`
(classification-function unit tests, acceptance criterion 1);
`tests/property/test_communications_writer.py` (Hypothesis property
tests through the real `EmailSendAuthorizer`/`CalendarEventAuthorizer`/
`AuthorizationOrchestrator` path, mirroring `tests/property/test_memory_writer.py`
exactly -- a `Classification.SECRET` body/attendee-bearing summary is
denied unconditionally under every real `PolicyContext`, including
`physical_confirmation_available=True`, acceptance criteria 2/3; a
dedicated test with 2-5 real recipients proves the SECRET-denial holds
regardless of recipient count -- ADR-0057's own "all-or-nothing, no
partial send" amendment, acceptance criterion 7); real adapter unit
tests for `send_message` (SMTP headers/body/login/quit sequencing) and
`create_event` (dates/summary/attendee passthrough, `mailto:` prefixing,
no-attendees passes `None` not `[]`, the missing-uid error path);
kernel-level tests proving a granted send/create genuinely reaches the
real port and a denied one never does (acceptance criterion 6's own
"granted reaches the port" half -- the real, live, skipif-guarded test
against a real mailbox/calendar remains unmet, no real test-account
credentials configured anywhere in this environment, matching every
other network-dependent adapter's own established precedent).

**Real, explicitly-named gaps, none rounded up to "done"**: at the
time this section was written, no real caller (CLI or voice) invoked
either write capability -- the identical "authorizable and proven, but
no wired entry point a person could actually use today" gap
`coding.run_task`/`job_assistance.draft` already have. `authorize_and_send_email`'s
own half of that gap has since closed -- see "CLI wiring: `send-email`
(2026-09-03)" immediately below. `authorize_and_create_calendar_event`
still has none. `email_port`/`calendar_port` still have no default,
the same "no implicit default for genuinely undecided per-deployment
config" precedent; no console-line wiring for either write capability
(M6a's own reads still lack this too, an existing, not new, gap); the
real `caldav`-vs-alternative library evaluation remains real, deferred
work, unrevisited by this pass.

### CLI wiring: `send-email` (2026-09-03)

`jarvis send-email` (`cli/main.py`) is now the first real caller of
`authorize_and_send_email`, closing that half of the gap named
immediately above. A flat, top-level subcommand -- matching
`ping`/`read`/`play`'s own granularity (one capability, one
subcommand), not `memory`'s nested-group shape, since only this one
write capability was wired, not a family introduced together. Four
new, required flags with no default (`--imap-host`/`--smtp-host`/
`--username`/`--password-reference`) construct a real `ImapEmailAdapter`
right there in `cli/main.py` -- the same real, per-deployment-config
reasoning `email_port`'s own docstring already states, and the CLI's
only other precedent for constructing a real, concrete adapter itself
rather than letting a kernel function default one internally
(`listen`'s own `Gtk4PhysicalConfirmationAdapter`). `SecretServiceAdapter()`
(real, default-constructible, no config needed) resolves the password
reference. Since `authorize_and_send_email` is `async`
(`EmailPort.send_message` is), this is the second place in `cli/main.py`
(after `listen`) that wraps a kernel call in `asyncio.run` -- every
other subcommand calls a synchronous kernel function directly.
`SecretNotFoundError` (a bad `--password-reference`) was added to
`main`'s existing exception-to-clean-message mapping, matching every
other real, catchable operational failure this file already handles.
Real tests (`tests/unit/test_cli_main.py`) prove routing (`to`/
`--subject`/`--body` reach the kernel call unchanged), real adapter
construction from the four new flags, decision printing on both
granted and denied outcomes, and the `SecretNotFoundError` clean-message
path -- all hermetic, `authorize_and_send_email` itself monkeypatched,
mirroring every other subcommand's own established test shape in this
file.

### CLI wiring: `create-calendar-event` (2026-09-03)

The identical wiring for `authorize_and_create_calendar_event`, built
immediately after `send-email` in a separate, later pass, per the
user's own direct request ("wire authorize_and_create_calendar_event
into the CLI too"). `--caldav-url`/`--username`/`--password-reference`
construct a real `CalDavCalendarAdapter`, the same "no default,
real per-deployment config" reasoning `send-email`'s own flags already
established. `--attendee` is repeatable (`action="append"`, default
`[]`) -- an event with zero, one, or several real attendees is the
same real distinction `calendar_effect_for` itself branches on. A
granted create's real new `uid` is printed (`uid: <value>`), mirroring
`memory write`'s own `identifier:` line; nothing is printed on a
denied decision, matching every other optional-payload print in this
file (`content`/`memory_identifier`/`memory_records`, all gated on
`is not None`).

**A second `PLR0912`/`PLR0915` complexity pressure point, resolved the
identical way `_run_memory_subcommand` already resolved it for
`memory`'s own four subcommands**: adding `create-calendar-event`
inline alongside `send-email` in `main`'s own `if`/`elif` chain would
have pushed it past ruff's branch-count threshold, and adding both
subcommands' `argparse` wiring inline in `_build_parser` would have
pushed that function past its own statement-count threshold. Both were
split out for real, not to silence a lint rule cosmetically:
`_add_communications_parsers` (parser construction) and
`_run_communications_subcommand` (dispatch, returning
`(decision, calendar_event_uid)`) are the same "split out to keep the
caller's own branch/statement count down" pattern `_run_memory_subcommand`
already established -- `main` itself now has a single
`elif args.command in ("send-email", "create-calendar-event"):` branch
for both.

`CalendarEventCreationError` (the real, checkable "server returned no
UID" anomaly `adapters/calendar.py` already defines) was added to
`main`'s exception-to-clean-message mapping, the identical treatment
`SecretNotFoundError` already got for `send-email`. Real tests
(`tests/unit/test_cli_main.py`) prove routing (summary/start/end/
attendees, including the repeated-`--attendee`/no-attendee cases),
real adapter construction, decision-plus-uid printing on grant,
decision-only printing (no stray `uid:` line) on denial, and the
`CalendarEventCreationError` clean-message path -- all hermetic,
`authorize_and_create_calendar_event` itself monkeypatched.

Both M6a write capabilities now have a real CLI caller. No voice
grammar was added for either -- out of scope for both passes, not
silently dropped.

## Dependency and security audit (2026-09-03)

A real vulnerability scan, not asserted or skipped: `pip-audit` 2.10.1
(installed for this pass, not previously part of this project's own
tooling) run against every real, resolved, installed package in this
project's own `.venv` -- both direct dependencies (`pyproject.toml`'s
own `dependencies` list, including `caldav`, added this same overnight
pass) and every real transitive dependency they pull in (214 total
packages, per `uv export`'s own resolved count). **Real result: no
known vulnerabilities found.** `jarvis` itself is correctly skipped
(not published to PyPI, nothing to look up) -- not a gap, the expected
outcome for this project's own package.

**A real, environment-specific quirk hit and worked around while
running this, named plainly**: `pip-audit`'s own default mode
(auditing a `requirements.txt` exported via `uv export`) failed with a
real dependency-resolution error unrelated to any vulnerability --
`backports-strenum==1.3.1` (a real, already-correctly-installed
transitive dependency of `ai-edge-litert`) reported `Requires-Python
>=3.8.6,<3.11` to `pip-audit`'s own dry-run resolver, even though the
exact same pinned version is genuinely installed and imports cleanly
under this project's real Python 3.12 environment -- a real PyPI
metadata/pip-audit interaction quirk, not a real incompatibility (the
package's own installed wheel has no such restriction; only the
metadata pip's resolver consulted did). Worked around by pointing
`pip-audit` directly at this project's own real, already-resolved
`.venv` (`PIPAPI_PYTHON_LOCATION`) instead of re-resolving from a
requirements file -- auditing what is actually installed and running,
not a hypothetical fresh install. `pip` itself (absent from this
project's own `uv`-managed venv by default) was temporarily installed
to let `pip-audit` introspect it, then removed immediately after --
confirmed via `git status` that this left no trace in `pyproject.toml`/
`uv.lock`.

**Unused-dependency check, confirmed via direct grep, not assumed**:
every package in `pyproject.toml`'s own direct `dependencies` list
(`jeepney`, `openwakeword`, `ai-edge-litert`, `sounddevice`, `numpy`,
`platformdirs`, `onnxruntime`, `faster-whisper`,
`nvidia-cublas-cu12`/`nvidia-cudnn-cu12`, `piper-tts`, `pygobject`,
`fastembed`, `websockets`, `caldav`) was checked against real,
grep-confirmed usage in `src/jarvis/`. **None were found unused** --
several (`faster_whisper`, `openwakeword`, `piper`, `fastembed`,
`caldav`) are lazily imported inside function bodies, matching this
project's own established "lazy-import a real-hardware/real-network
client" precedent, and did not show up under a naive top-of-file
import grep; each was re-checked and confirmed genuinely used before
being ruled out as a false lead, not assumed unused from the first,
too-narrow grep pass alone. No dependency was removed -- there was
nothing real to remove.

**No code changes resulted from this audit** -- a clean scan, at this
real point in time, with the real tool/version/scope stated precisely
rather than left implicit. Not a standing guarantee: this is a
point-in-time result, not re-run automatically by CI (no
`pip-audit`/equivalent step exists in `.github/workflows/ci.yml` as of
this pass) -- a real, named gap for a future pass to consider closing,
not silently assumed to already be continuous.

## Real, found gap: does ADR-0057's `Tier.CONFIRM` satisfy the charter? (2026-09-03)

Investigated directly, per the user's own explicit request, after
`jarvis send-email`/`jarvis create-calendar-event` were already
merged and CLI-invocable. Not assumed either direction; the real code
was read and quoted before drawing any conclusion.

**Question 1 -- can `Tier.CONFIRM` be satisfied by remote confirmation
alone, for these two capabilities specifically? Yes, confirmed
directly in code.** `domain/policy.py::evaluate()`:

```python
elif tier == Tier.CONFIRM:
    granted = context.physical_confirmation_available or context.remote_confirmation_available
```

versus `Tier.MANUAL_ONLY`, three lines above, which deliberately never
reads `remote_confirmation_available` even when `True`. `Effect.EGRESS_SENSITIVE`
(what `authorize_and_send_email`/`authorize_and_create_calendar_event`
declare for every non-`SECRET` invocation) floors at `Tier.CONFIRM`,
not `Tier.MANUAL_ONLY`, per `domain/capability.py`'s own
`_EFFECT_TIER_FLOOR`. Already directly, deliberately tested this way,
not merely inferable: `tests/property/test_communications_writer.py`'s
own `test_non_secret_email_bodies_are_never_denied_by_send_alone`/
`test_non_secret_attendee_bearing_summary_floors_at_confirm` both
assert `expected_granted = physical_confirmation_available or remote_confirmation_available`
against the real `EmailSendAuthorizer`/`CalendarEventAuthorizer` --
written and passing before this investigation, proving the behavior
being questioned was already deliberately built this way, not a bug.

**This is live today, not theoretical**: `jarvis send-email ...
--remote-confirmation-available` (no `--physical-confirmation-available`)
already authorizes a real SMTP send via the real CLI wiring merged
earlier the same day. `ManualConfirmationAdapter`
(`adapters/confirmation.py`) sharpens this further -- both booleans are
self-declared CLI flags with zero real presence/hardware verification
behind them today, by its own docstring's own admission ("Real
presence/hardware detection is future work"). "Remote confirmation"
currently means nothing more than a caller typing one flag.

**Question 2 -- did ADR-0057 or `m6a-communications.md` ever check
this reuse against the charter's own explicit language for email
specifically? No.** A direct search of both documents for
`physical`/`MANUAL`/`desktop`/`charter` found exactly one relevant
passage, in ADR-0057's own Consequences section -- and it treats the
question as speculative future policy, not a present requirement:
"if that distinction ever becomes real, necessary policy (e.g., a
future decision that email should require `MANUAL_ONLY` while
cloud-provider egress stays `CONFIRM`), it would need its own new
effect at that point." ADR-0057 named this exact possibility and
deferred it as an "if" -- without checking it against the project's
own founding charter, which names "sending emails" explicitly among
actions requiring "manual confirmation through the desktop interface,"
never voice/remote alone. That charter language is not currently
reproduced verbatim anywhere in this repo's own committed docs
(confirmed by direct grep across `CLAUDE.md` and `docs/`) -- it is the
system's own original, external founding description, referenced but
not quoted by `CLAUDE.md`'s own condensed "Destructive/irreversible/
credential actions always require `MANUAL_ONLY`" principle and by
ADR-0019's own Decision (whose own `Source` field is literally "CLAUDE.md
architecture summary, Milestone 0").

**`Effect.EGRESS_SENSITIVE` was calibrated for a different, narrower
risk.** ADR-0038's own Context: `EGRESS_SENSITIVE`/`EGRESS_SECRET`
were built for "M2's `ReasoningPort` adapters... the first
capabilities in this repo whose whole purpose is sending data to a
cloud provider" -- a cloud AI model seeing text, not a real, external
human receiving a real, unrecallable email or calendar invite.
ADR-0057 reused this floor by analogy to that narrower original
purpose (sound for the `SECRET`/`DENY` half -- an email can never
carry a `SECRET` value, matching cloud-egress's own zero-tolerance --
but never independently re-derived for the `CONFIRM`-tier,
remote-satisfiability question).

**ADR-0019's own real criteria is effect-based, not charter-text-based**:
"Any capability whose declared effects include DESTRUCTIVE, IRREVERSIBLE,
or CREDENTIAL is hard-pinned to `MANUAL_ONLY`." `Effect.EGRESS_SENSITIVE`
is not a member of that set. Two of the charter's own four named
examples ("deleting files," "changing passwords") map cleanly onto
this existing floor already; a third ("submitting applications") was
independently, directly resolved by the user as M6b's own *structural*
boundary (ADR-0058) -- stronger than `MANUAL_ONLY`, not weaker. The
fourth, "sending emails," is the one the charter names explicitly by
name, and it is the one this codebase currently classifies at
`Tier.CONFIRM`, remote-satisfiable.

**Conclusion: a real, evidenced gap existed.** Written up as
[`docs/adr/0059-email-and-attended-calendar-event-confirmation-tier-may-not-satisfy-the-charter.md`](../adr/0059-email-and-attended-calendar-event-confirmation-tier-may-not-satisfy-the-charter.md)
-- drafted Proposed, three real options laid out without a preference
baked in, matching this project's own established precedent of routing
decisions of this shape to the user directly rather than resolving
them remotely (ADR-0058's own "no auto-apply" boundary). No code was
changed as part of the investigation itself -- the finding was real
and live, but the fix was exactly the kind of first-principles
product-safety decision this project has consistently reserved for the
user. See "ADR-0059 Accepted, gap closed for real (2026-09-03)"
immediately below for what happened next.

## ADR-0059 Accepted, gap closed for real (2026-09-03)

**The user chose directly, in the same conversation, immediately
after the investigation above was reported**: email-send and
attended-calendar-event creation require `Tier.MANUAL_ONLY`, the same
floor `git.force_push`/`memory.forget` already use -- option (a) of
the three ADR-0059 laid out, with the specific shape resolved too
(reuse the existing `Effect.DESTRUCTIVE | Effect.IRREVERSIBLE`
combination, not a new `Effect` member). ADR-0059's own `## Status`
now records the real acceptance quote and the real choice among its
own three options, mirroring ADR-0058's own acceptance-record
precedent.

**The real code change, not just the record**:
`application/communications/classification.py::egress_effect_for` now
returns `Effect.DESTRUCTIVE | Effect.IRREVERSIBLE` for every non-`SECRET`
classification, in place of `Effect.EGRESS_SENSITIVE` -- the
`Classification.SECRET` -> `Effect.EGRESS_SECRET`/`Tier.DENY` branch is
untouched, unaffected by this ADR either way. `calendar_effect_for`
needed no code change -- it already delegates to `egress_effect_for`
for the attendee-bearing branch, so the new floor applies
automatically; the attendee-less branch (`Effect.WRITE_LOCAL`/`Tier.CONFIRM`)
is untouched, exactly as ADR-0059 itself specified. Neither
`EmailSendAuthorizer` nor `CalendarEventAuthorizer`
(`application/communications/writer.py`) needed to change -- the
effect substitution is entirely contained in the classification
function both already call, the same reason the domain-level property
test suite (`tests/property/test_capability.py`) needed no new test
either: `Effect.DESTRUCTIVE`/`Effect.IRREVERSIBLE` already have their
own real, existing `>= Tier.MANUAL_ONLY` property proof
(`test_high_risk_effect_always_at_least_manual_only`), covering this
new caller automatically, the same way `Effect.EGRESS_SECRET`'s own
existing `DENY` property test already covered these two capabilities
without a new domain-level test when ADR-0057 first landed.

**The single most important test in this pass, real and passing**:
`tests/property/test_communications_writer.py`'s own
`test_non_secret_email_bodies_require_physical_confirmation_specifically`/
`test_non_secret_attendee_bearing_summary_requires_physical_confirmation_specifically`
(replacing `test_non_secret_email_bodies_are_never_denied_by_send_alone`/
`test_non_secret_attendee_bearing_summary_floors_at_confirm`, which
were wrong under the new floor and were not left contradicting the
real code) each assert
`decision.granted == context.physical_confirmation_available` under
every real, Hypothesis-generated `PolicyContext` -- mirroring
`tests/property/test_policy.py`'s own
`test_manual_only_requires_physical_confirmation_specifically` exactly.
Confirmed passing for real, not merely written:

```
$ uv run pytest -q tests/property/test_communications_writer.py
13 passed
```

`tests/unit/application/communications/test_classification.py`'s own
non-`SECRET` cases were updated the same way (real unit-level proof of
the classification function's own new return value, not just the
authorizer-level property). `tests/unit/test_communications_kernel.py`
gained two new, explicit end-to-end denial tests
(`test_send_email_with_only_remote_confirmation_is_denied`,
`test_create_calendar_event_with_attendees_and_only_remote_confirmation_is_denied`)
proving the same property through the real composition-root functions
(`authorize_and_send_email`/`authorize_and_create_calendar_event`),
plus one confirming the attendee-less case remains genuinely
unaffected (still grantable by remote confirmation alone, matching
`git.push`'s own precedent, untouched by this ADR). Every existing
granted-path test (which already used
`physical_confirmation_available=True`) needed no change -- they were
already correct under the new floor, not merely lucky; nothing was
found silently passing for the wrong reason.

**Full gate suite confirmed green after the change**: `ruff check`,
`ruff format --check`, `mypy --strict` (329 source files), `lint-imports`
(6 contracts kept), `pytest` (1041 passed, 1 skipped, the one known
pre-existing display-dependent failure unrelated to this change), and
all three 100%-coverage gates (`domain/`, `application/policy/`,
`application/reasoning/`).

**What this closes and what it does not, stated precisely**:
`jarvis send-email --remote-confirmation-available` (no
`--physical-confirmation-available`) -- which authorized a real SMTP
send before this change -- is now denied. This closes the *tier* gap
this ADR exists to fix. It does **not** close the separate,
pre-existing gap this section's own investigation already named:
`physical_confirmation_available` itself remains a self-declared CLI
flag with no real presence/hardware verification behind it anywhere in
this codebase (`ManualConfirmationAdapter`'s own documented "future
work" gap) -- a real caller today can still type
`--physical-confirmation-available` with nobody physically present.
That gap is real, pre-existing (not introduced or worsened by this
ADR), and remains open, named here rather than conflated with the gap
this pass actually closed. **Voice grammar for
`send_email`/`create_calendar_event` remains explicitly out of
scope for this pass** -- closing the `Tier.CONFIRM` gap does not, by
itself, mean voice grammar is now safe to add; that remains a real,
separate, future decision, per the user's own explicit instruction.

## Overnight Track 1: real, local-only default providers for coding.run_task/job_assistance.draft (2026-09-04)

Verified directly before building, not assumed. `adapters/reasoning/local.py`'s
`LocalReasoningAdapter` calls `http://localhost:11434/api/generate`
(Ollama's real REST shape) via stdlib `urllib.request` -- `localhost`
only, never a cloud host. No `SecretPort` dependency anywhere in the
module: a local server on the loopback interface needs no credential.
Quoted directly from the real code: `_ENDPOINT = "http://localhost:11434/api/generate"`,
and the adapter's own constructor takes no API key, account, or
credential parameter of any kind.

**Live-verified for real, not left theoretical**: `ollama` was already
installed on this development machine (`~/.local/bin/ollama`) but no
server was running and no model pulled. `ollama serve` was started for
real; `qwen2.5:0.5b` (397MB) was pulled for real -- an anonymous,
public registry pull, no account or API key of any kind, matching the
hard gate's own "no real cloud-provider credential" requirement
exactly, since this is not a cloud reasoning provider at all.
`LocalReasoningAdapter(model="qwen2.5:0.5b").generate(...)`, called
directly, returned a real generated response. `authorize_and_run_coding_task`
and `authorize_and_draft_document`, called with no explicit
`dispatcher_factory`/`providers` argument, both genuinely reached the
real local server end to end and returned real, non-`None` results --
proven by two new, skipif-guarded live tests
(`tests/unit/test_coding_kernel.py::test_real_default_dispatcher_factory_reaches_a_locally_running_ollama_server`,
`tests/unit/test_job_assistance_kernel.py::test_real_default_providers_reaches_a_locally_running_ollama_server`),
both passing on this machine tonight, both honestly skipped in CI
(no Ollama server there, matching this project's own established
"real infrastructure, not assumed in CI" precedent for
`test_real_cdp_flow_against_a_local_page`/`test_real_imap_smtp_flow_against_a_configured_mailbox`).

**What changed, precisely**: `kernel/coding.py`'s `authorize_and_run_coding_task`'s
`dispatcher_factory` parameter and `kernel/job_assistance.py`'s
`authorize_and_draft_document`'s `providers` parameter both gained a
real default (`None` resolves to `_local_only_dispatcher_factory(orchestrator)`/
`_local_only_providers()` respectively, both of which already existed
as real, tested, credential-free helpers -- only unused as the actual
default before tonight). **A real correction to this project's own
prior reasoning, named plainly**: both modules' own docstrings
previously stated "no default, on purpose," citing the risk of
inventing an undecided real cloud-provider default -- true, but it
conflated that real concern with a separate question ("should any
default exist at all") the local-only helper had already answered
safely. Fixed on the user's own direct instruction, after they
independently re-read both modules and confirmed this precisely.
**What remains unchanged**: no real cloud-provider default was
invented; an explicit `dispatcher_factory`/`providers` argument still
reaches real cloud escalation for a caller that supplies one, and no
cloud API key, account, or credential was configured or referenced
anywhere in this pass, matching the hard gate exactly.

`_DEFAULT_MODEL` in `adapters/reasoning/local.py` changed from the
placeholder string `"local-default"` (never a real, resolvable Ollama
tag) to `"qwen2.5:0.5b"` (the real, live-verified model). **Real,
honest quality trade-off, stated plainly, not hidden**: `qwen2.5:0.5b`
is a genuinely small model (0.5B parameters), chosen for its small
download and fast response on an overnight, unattended pass, not for
output quality. A caller relying on this default -- via `jarvis code`/
`jarvis draft`, both new, real CLI subcommands wired tonight -- should
expect materially weaker generations than any real cloud provider
would give; the real coding-loop live test above deliberately asserts
only that a real, non-`None` result comes back, not that the model
produces a *working* patch, which is not a fair bar for a
500M-parameter model. A future deployment wanting better local quality
overrides `model=` with its own larger, real, pulled Ollama tag --
nothing in this change prevents that.

Neither CLI subcommand exposes a cloud-provider override flag, by
design: building one would mean wiring a real vendor-family adapter,
model choice, and keyring secret reference here -- exactly the real,
unattended cloud-provider configuration this pass's own hard gate
forbids. A caller wanting real cloud escalation calls
`authorize_and_run_coding_task`/`authorize_and_draft_document`
directly with an explicit override, bypassing the CLI.

## Overnight Track 2: real file management -- fs.list_dir/fs.move_file/fs.delete_file, ADR-0060 (2026-09-04)

Verified directly before building, not assumed: the user's own
independent inspection of `ports/file_system.py` tonight found it 39
lines long, exposing a single `read_text` method, its own module
docstring stating write/list/delete were deliberately not yet built --
the one real charter-completeness gap Track 6's own predecessor pass
had already surfaced. This pass closes it for real, not with another
proposal document.

**What was built.** `FileSystemPort` (`ports/file_system.py`) gained
three methods: `list_dir(path) -> tuple[DirEntry, ...]` (directory
entries, not recursive), `move(source, destination) -> None`
(files and directories, native `shutil.move` semantics), `delete(path)
-> None` (files only -- confirmed directly on this machine, not
assumed, that `Path.unlink()` raises `IsADirectoryError` for a
directory, so no directory-deletion path silently exists).
`LocalFileSystemAdapter` (`adapters/file_system.py`) implements all
three with native `pathlib`/`shutil` calls, no new safety logic
invented, matching `read_text`'s own "let real exceptions propagate"
precedent. `domain/file_system.py` adds `DirEntry` (`name: str,
is_dir: bool`), stdlib-only, matching `PageHandle`'s own "explicit
typed fields, not one opaque blob" precedent.

Three new capabilities registered in `kernel/capabilities.py`:
`fs.list_dir` (`Effect.EGRESS_LOCAL`), `fs.move_file`
(`Effect.WRITE_LOCAL`), `fs.delete_file` (`Effect.DESTRUCTIVE |
Effect.IRREVERSIBLE`) -- floors `Tier.ALLOW`/`Tier.CONFIRM`/
`Tier.MANUAL_ONLY` respectively via the existing `_EFFECT_TIER_FLOOR`
mapping, no new mapping entries needed. `kernel/files.py` gained
`authorize_and_list_dir`/`authorize_and_move_file`/
`authorize_and_delete_file`, mirroring `authorize_and_read_file`'s own
shape exactly: scope-check via the existing `_resolve_within_scope`
(default `allowed_root=Path.home()`, unchanged) before authorization,
authorize via `authorize_by_id`, only touch the real filesystem on
grant, always append the `Decision` to the audit chain. `fs.move_file`
scope-checks *both* `source` and `destination` before authorizing --
proven by a dedicated test
(`test_move_file_rejects_an_out_of_scope_destination`) that a
same-root source with an out-of-scope destination is rejected before
any real filesystem call.

**The protected-path question, ADR-0060's own real decision, not a
silent omission.** Two obvious alternatives were considered and
rejected in writing, not silently passed over: reusing ADR-0056's
`resolve_protected_patterns` (rejected -- its real purpose is
detecting a *target repository's own* test-file convention so an
*autonomous coding agent* cannot corrupt the tests that would catch
its own bad patches; general file management has no target repo, no
test-file convention, and no agent writing unsupervised on a human's
behalf, so reuse would be a category error, not a reasonable
extension); a bespoke denylist of sensitive subpaths (rejected --
`fs.read_file`'s own docstring already states and applies this
project's "allowlist-over-denylist posture," explicitly naming a
denylist as creating "a false sense of completeness"; adding one now
for move/delete would contradict an already-reasoned position with no
new justification found). The real, positive reasoning for "no
additional check, the existing `allowed_root` boundary is enough":
`Tier.MANUAL_ONLY` requires real, physical, per-invocation human
confirmation for `fs.delete_file` -- the user sees exactly which path
is about to be permanently removed and can refuse, every single time.
ADR-0056's own protected-path check exists specifically *because* the
coding agent's real writes never get that per-write checkpoint (only
one outer `Effect.EXECUTE`/`Tier.CONFIRM` gate on invoking the agent
at all); `fs.delete_file` never has that gap. `fs.move_file` floors at
the weaker, remote-satisfiable `Tier.CONFIRM` deliberately -- moving is
real but reversible, nothing is lost, the same asymmetry
`ports/file_system.py`'s own module docstring already states.

**ADR-0060 status: Proposed, not Accepted**, mirroring ADR-0055's/
ADR-0056's own "build and prove first, accept after" sequencing --
this pass's own hard gate explicitly forbade marking it Accepted.
Awaiting the user's own direct review of the protected-path reasoning
above before any acceptance.

**Real tests, not just inspection.** Unit tests
(`tests/unit/test_files.py`) prove authorization-ordering for all
three -- denied requests never touch the real filesystem (checked via
call-tracking stub/raising doubles), granted requests do. A dedicated
property test
(`tests/property/test_files_kernel.py::test_delete_file_granted_tracks_physical_confirmation_alone`)
proves `fs.delete_file`'s `Decision.granted` equals
`physical_confirmation_available` alone across all four
`(physical, remote)` combinations Hypothesis generates -- the single
most important property this ADR depends on, mirroring
`test_manual_only_requires_physical_confirmation_specifically`'s own
shape exactly (ADR-0059 established this same pattern one pass ago).
A second property test confirms `fs.move_file`'s ordinary `Tier.CONFIRM`
floor (`physical OR remote`) is unaffected. Each Hypothesis example
uses a fresh, real `tempfile.TemporaryDirectory()`, not the
function-scoped `tmp_path` fixture, which would be silently stale or
reused across examples `@given` generates.

**Real CLI callers**: `jarvis list-dir <path>`, `jarvis move-file
<source> <destination>`, `jarvis delete-file <path>` (`cli/main.py`),
all using the same default `allowed_root` (`Path.home()`) `fs.read_file`
already uses -- no `--allowed-root` override flag exists for any of
the four `fs.*` subcommands now, matching `read`'s own existing
precedent, not a new inconsistency. `list-dir`'s granted output prints
each real entry's name, with a trailing `/` for directories.

**Real, deliberately named scope limits**: `fs.delete_file` removes a
single real file only -- recursive directory deletion is a real,
separate, more consequential decision this pass does not make.
`allowed_root`'s own scope boundary remains coarse (a user's home
directory still contains real project repositories, dotfiles, and
other sensitive-but-not-system-level content once granted) -- this ADR
treats `Tier.MANUAL_ONLY`'s physical-confirmation requirement as the
operative safeguard, not a path-pattern filter; if that judgment turns
out wrong in practice, a protected-path mechanism specific to general
file management, not reused from ADR-0056, would need its own new ADR.
No voice grammar was added for any of the three capabilities -- out of
this pass's own scope, matching Track 3's own separate, not-yet-started
status.
