# Threat Model

The system's threat model: trust boundaries, adversary assumptions, and
the mitigations behind the policy engine, provenance tainting, and
audit chain. Populated alongside the security-relevant work packages —
see the roadmap for the target work package.
