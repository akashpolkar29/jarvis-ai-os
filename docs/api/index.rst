JARVIS AI OS -- API Reference
==============================

Real, generated reference documentation for JARVIS's own source tree,
built from the docstrings already present in the code -- this page does
not add new prose, it surfaces what exists.

Scoped to the five packages the dependency rule names
(``domain -> ports -> application -> adapters -> kernel``); ``cli``,
``ipc``, ``plugin_api``, and ``ui`` are intentionally excluded, matching
this pass's own instruction.

.. toctree::
   :maxdepth: 2
   :caption: Packages

   reference/jarvis
