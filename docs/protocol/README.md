# Protocol

The wire protocol used by `jarvis.ipc` for clients (CLI, future voice
frontend, future GUI) to talk to a running kernel instance. Populated
once `jarvis.ipc` exists — see the roadmap for the target work package.
