# Architecture

This directory will hold the approved, frozen architecture documents
(`m0-architecture.md`, `m2-reasoning-layer.md`, `system-design.md`,
`roadmap.md`) referenced by `CLAUDE.md`. They are provided separately
and are populated as they're supplied, not authored as part of the
repository scaffold in work package 1.
