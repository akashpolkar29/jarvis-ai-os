# Plugin Guide

Documentation for third-party capability plugin authors, built against
`jarvis.plugin_api`. Populated once the plugin API has real surface
area — see the roadmap for the target work package.
